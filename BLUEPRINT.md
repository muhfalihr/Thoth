# THOTH — Context Editing Blueprint

> **Petunjuk penggunaan:** File ini adalah *living document* — status diperbarui setiap ada implementasi baru.
> Selalu sertakan file ini sebagai konteks di setiap sesi kerja pada project THOTH.

---

## Tujuan Sistem

Membangun pipeline otomatis yang memahami **gaya editing media sosial viral** (TikTok/Reels/Shorts) secara end-to-end: dari ingestion video mentah hingga produksi clip yang mengikuti tren editing terkini, dengan bantuan LLM sebagai synthesis engine.

---

## Status Keseluruhan

| Layer | Coverage | Keterangan |
|-------|----------|-----------|
| Ingest + transkrip | ✅ 100% | yt-dlp + Whisper word-level timestamps |
| Visual scoring (post-analysis) | ✅ 80% | Frame extraction + vision LLM scoring |
| LLM synthesis dari teks saja | ✅ 95% | Multi-provider, chunked, trending-aware; **LLM keyword extraction otomatis dari transcript** (gantikan word-frequency approach); `--keywords` CLI jadi optional override |
| Combined audio-visual prompt | ✅ 85% | Full-video frame descriptions diinjeksikan ke transcript (opt-in via `vision.describe_video`) |
| Production metadata (sfx/bgm/style) | ✅ 85% | LLM pilih per clip, auto-discovery katalog |
| **Annotated asset catalog → LLM cues** | ✅ 100% | `scripts/annotate_assets.py` anotasi SFX/meme/font (5-beat + trigger, audio diukur, video via vision); `analyze/asset_catalog.rs` inject ke prompt; schema `asset_cues[]` ber-timestamp diisi LLM + divalidasi. **Render audio**: `build_cue_audio_filter`/`build_delayed_audio_filter` mix multi-SFX (`AssetSfxCue`). **Render video**: `build_meme_overlay_filter` meme PiP di pojok bergilir (`MemeCue`). **Audio meme**: meme bersuara (katalog `has_audio`) di-mix + narasi di-duck 50% di window-nya. **Beat-snap**: at_sec cue di-snap ke beat BGM saat beat_sync. Semua divalidasi via ffmpeg run + frame visual |
| Trend awareness (real-time) | ✅ 70% | Google Trends + keyword scoring |
| **Style Profiles system** | ✅ 100% | `StyleProfile` struct + `StylesConfig` di config.rs; 4 default profiles di config.toml; apply di edit/service.rs |
| **CapCut-style subtitle animation** | ✅ 100% | `SubtitleStyle` enum (Karaoke/CapcutBold/WordPop/MinimalWhite) di subtitle.rs; LLM pilih per clip via `subtitle_style` field |
| **Hook title multi-warna (Beat-1)** | ✅ 100% | `edit/hook_title.rs` generate ASS judul raksasa bertumpuk, tiap kata warna palette, font Montserrat-ExtraBold, animasi pop scale-bounce; di-burn sbg subtitles pass kedua (`AudioOptions.hook_title_ass`); `[hook_title]` config opt-in; divalidasi render+frame visual |
| **Profile card + nama di atas kepala (Beat-2)** | ✅ 100% | `edit/profile_card.rs` render kartu (avatar inisial + nama + @handle + stats) & nama raksasa di atas kepala via drawbox/drawtext (tanpa input/aset); LLM isi `character_name/handle/stats` (schema); `[profile_card]` opt-in; divalidasi render+frame. Stats hanya bila disebut transcript (no fabrikasi). ✅ **Avatar foto ASLI + handle/follower faktual via scout** (`content_profile.json` → override LLM, composite foto via `ImageBadgeCue`) — entry (v) |
| **Callout angka + panah (Beat-3)** | ✅ 100% | `edit/callout.rs` render angka di kotak accent + glyph panah ◀▶▲▼ via drawtext (tanpa aset PNG); LLM isi `callouts[]` (text/at/posisi/arah) di schema; `[callout]` opt-in; divalidasi render+frame visual |
| **Animelorian composite mode** | ✅ 100% | Base kanvas kertas hitam kusut + footage sbg KARTU tengah + montase (cut main↔enrichment) + hook full-frame. `AnimelorianConfig` (`[animelorian]` opt-in); `build_video_filter` branch paper-canvas; `OverlayStyle::FootageCard` + arm di `build_overlay_filter` (enable-window, `shortest=1` utk bound paper loop); enrichment pool drive cutaway card. Divalidasi full-pipeline + frame visual (hook immersive, konten paper+card, montage cut bersih) |
| **Reference video style analyzer** | ✅ 100% | `TrendAnalyzeService` di trend_analyzer.rs; download video → extract frames → vision LLM → synthesize profile → save TOML |
| **Query keyword expansion** | ✅ 100% | LLM analisis raw query → 5-8 keyword pencarian ganda sebelum multi-platform search (mis. "kata asbun ghufron al maqoli" → "ghufron viral","ghufron ceramah","MUI ghufron",..). Memperluas footage + memperkaya enrichment pool → konteks multi-sumber utk narasi rage-bait. `expand_query()` di `src/ingest/query_expand.rs`; `ContentSearchConfig.expand_keywords` (`[content_search]` opt-in, default true); dipanggil di `resolve_query_to_url()` (main.rs) sebelum `search_content()`; graceful fallback ke raw query bila LLM gagal. `src/ingest/query_expand.rs` |
| **Trend-aware editing engine** | ❌ 0% | Full adaptive auto-learning dari TikTok Creative Center + YouTube Trending — belum diimplementasi (Priority 6) |
| RAG / knowledge base viral patterns | ✅ 95% | Supabase pgvector + embedding; store & retrieve moments; inject examples ke prompt; video_title/channel kini diteruskan ke storage |
| Beat-sync audio-visual | ✅ 100% | `beat_detect.rs` BPM dari metadata; SFX snap ke downbeat; BGM ducking; ClipStyle duration = beat subdivision |
| Scene boundary detection | ✅ 100% | `detect_scene_boundaries()` via FFmpeg select filter, opt-in via `vision.scene_detection` |
| **News Enrichment (Stage 4) — Phase 1-6** | ✅ 100% | Keyword extraction + search + screenshot + 9:16 format + video overlay + reaction script (LLM) + TTS (Edge TTS) + static avatar post-roll + SadTalker lip-sync talking avatar (local GPU, tidak butuh API). |
| **Narration cultural-context enrichment** | ✅ 100% | scout `enrich_context.js` decode subteks komentar (references entitas/meme/slang + per-comment context + discourse) → blok `[Konteks Budaya]`/`[Maksud Komentar]` di narasi; web-grounding status terkini (Google News via CDP, `web_grounding.js`); CKB cache di **Supabase** (`ckb.js`, fallback lokal-JSON); narator diinstruksi baca sarkasme & tak menyalahkan netizen. `enrich_context.js`/`web_grounding.js`/`ckb.js`, `pipeline/mod.rs`, `narration/mod.rs`, `ingest/content_search.rs` |
| **Cultural Pulse (trend dari diskursus)** | ✅ 80% | `pulse_harvest.js` (cron harian): scrape komentar feed trending → distilasi term berulang + register gaya bahasa → `ckb_pulse` (recency-decay) → blok `[Tren Diskursus]` + flavor register opsional. Sumber = komentar (discourse), bukan view-index. Pelengkap Priority 6. |

---

## Blueprint Detail & Status Implementasi

---

### 1. Definisi Metrik 'Context Editing'

Parameter yang harus diekstraksi dan dipahami LLM untuk menilai kualitas editing.

| Parameter | Status | Implementasi | File |
|-----------|--------|--------------|------|
| **Pacing & Transisi** | ✅ Implemented | `ClipStyle` enum (fade/flash/zoom/smooth/none), LLM memilih per clip via `clip_style` field di `ViralMoment` | `src/edit/ffmpeg.rs` |
| **Struktur Hook (3 detik)** | ✅ Implemented | Field `hook` di `ViralMoment` — LLM identifikasi kata-kata pertama yang menarik | `src/analyze/schema.rs` |
| **Overlay & Caption Style** | ✅ Implemented | Headline lower-third panel (news-ticker) + subtitle karaoke kuning/putih + Poppins font | `src/edit/ffmpeg.rs`, `src/edit/subtitle.rs` |
| **Headline animation mengikuti style** | ✅ Implemented | `HeadlineAnim` — panel masuk/keluar sesuai ClipStyle (slide-up untuk Zoom, snap untuk Flash, dll) | `src/edit/ffmpeg.rs` |
| **Audio-Visual Sync (beat-sync)** | ❌ Not implemented | SFX selalu di t=0, belum ada deteksi BPM/beat drop untuk sinkronisasi transisi | — |
| **Viral type classification** | ✅ Implemented | 8 kategori: educational_shock, transformation, controversy, actionable, relatable, blueprint, inspiration, storytelling | `src/analyze/schema.rs` |
| **Emotional trigger classification** | ✅ Implemented | 8 kategori: curiosity, surprise, validation, inspiration, fear, humor, empathy, admiration | `src/analyze/schema.rs` |
| **Energy level** | ✅ Implemented | high/medium/low — digunakan untuk memilih BGM dan SFX | `src/analyze/schema.rs` |

---

### 2. Pipeline Ingestion & Pre-processing

Membedah file media menjadi komponen yang dapat diproses.

| Komponen | Status | Implementasi | File |
|----------|--------|--------------|------|
| **Download video (yt-dlp)** | ✅ Implemented | yt-dlp subprocess dengan progress streaming, auto-merge video+audio | `src/ingest/service.rs` |
| **Copyright detection** | ✅ Implemented | Cek field `license` + scan description untuk kata-kata copyright | `src/ingest/service.rs` |
| **YouTube transcript download** | ✅ Implemented | Auto-download subtitle YouTube (json3/vtt), fallback ke Whisper | `src/ingest/service.rs` |
| **Ekstraksi Audio (WAV)** | ✅ Implemented | FFmpeg → WAV 16kHz mono untuk Whisper | `src/edit/ffmpeg.rs` |
| **Frame Extraction** | ✅ Implemented | `vision.rs` — extract JPEG frames via FFmpeg per candidate moment | `src/analyze/vision.rs` |
| **Scene Boundary Detection** | ❌ Not implemented | Saat ini sampling uniform (fps = count/duration). Harusnya menggunakan FFmpeg `select='gt(scene,0.3)'` untuk deteksi pergantian adegan yang sesungguhnya | — |
| **Keyframe-only extraction** | ❌ Not implemented | Ambil frame di interval tetap, bukan hanya di scene cut yang signifikan | — |
| **Audio tempo/BPM detection** | ❌ Not implemented | Tidak ada analisis ritme musik BGM untuk sinkronisasi transisi | — |

---

### 3. Multimodal → Text Translation

Jembatan antara media audio-visual dan LLM teks.

| Komponen | Status | Implementasi | File |
|----------|--------|--------------|------|
| **Speech-to-Text + word timestamps** | ✅ Implemented | Whisper local CUDA (large-v3) + Groq Whisper API, word-level timestamps | `src/transcribe/service.rs` |
| **BPE subword merging** | ✅ Implemented | `fix_subwords()` / `merge_subword_tokens()` — gabungkan token terpecah seperti "ber"+"sama" | `src/transcribe/model.rs` |
| **Visual frame scoring (Vision LLM)** | ✅ Implemented | Frame → Gemini/Claude/OpenAI/Novita/OpenRouter/vLLM → skor humor, visual_impact, novelty, engagement (0-10) | `src/analyze/vision.rs` |
| **Multi-provider vision support** | ✅ Implemented | Gemini, OpenAI, Claude, Novita, OpenRouter (OpenAI-compat), vLLM (multimodal). `vision_active` whitelist di `service.rs` gates rerank+describe — Novita/OpenRouter kini termasuk (sebelumnya excluded → vision dead di config default) | `src/analyze/vision.rs`, `src/analyze/service.rs` |
| **Rich frame description per timestamp** | ✅ Implemented | `describe_video_frames()` di `vision.rs` — sample 1 frame per N detik, kirim ke vision LLM, hasilkan deskripsi teks per frame | `src/analyze/vision.rs` |
| **Combined audio+visual synchronized prompt** | ✅ Implemented | `build_enriched_transcript()` menginjeksikan deskripsi visual ke setiap baris transcript. Aktifkan via `vision.describe_video = true` | `src/analyze/vision.rs`, `src/analyze/service.rs` |

**Output yang kini tersedia (dengan `describe_video = true`):**
```
[120] "Hampir 60% uang dari Cina!"
  ↳ Visual: close-up wajah pembicara, ekspresi serius, grafik merah di background
[130] "Dan ini yang tidak pernah diberitahu..."
  ↳ Visual: pembicara menunjuk grafik, tangan menekankan angka di layar
```

---

### 4. LLM Synthesis Engine

Core engine yang menganalisis dan menghasilkan output terstruktur.

| Komponen | Status | Implementasi | File |
|----------|--------|--------------|------|
| **Multi-provider LLM** | ✅ Implemented | Groq, OpenAI, Claude, Gemini, vLLM, Ollama | `src/analyze/provider/` |
| **Structured JSON output (ViralMoment)** | ✅ Implemented | 17+ fields: title, headline, hook, caption, viral_type, sfx_vibe, bgm_vibe, overlay_query, dll | `src/analyze/schema.rs` |
| **Transcript + timestamp ke LLM** | ✅ Implemented | Format `[sec] teks` dikirim ke LLM dengan keyword markers | `src/analyze/prompt.rs` |
| **Chunked analysis (long videos)** | ✅ Implemented | Auto-chunking 180s windows dengan 30s overlap untuk video >15 menit | `src/analyze/service.rs` |
| **Retry + JSON repair** | ✅ Implemented | Retry sekali dengan fresh prompt jika JSON invalid | `src/analyze/service.rs` |
| **Visual re-ranking post-analysis** | ✅ Implemented | Vision scores digunakan untuk re-rank kandidat (text 65% + visual 35%) | `src/analyze/service.rs` |
| **AI-generated headline (news-ticker)** | ✅ Implemented | Field `headline` terpisah dari `title` — dirancang untuk visual overlay, bukan SEO | `src/analyze/schema.rs` |
| **Per-clip production suggestions** | ✅ Implemented | LLM memilih clip_style, sfx_vibe, bgm_vibe, overlay_query per moment | `src/analyze/schema.rs` |
| **Combined audio+visual synthesis** | ✅ Implemented | Frame descriptions diinjeksikan ke transcript sebelum main LLM analysis. `vision.describe_video = true` untuk aktifkan | `src/analyze/service.rs` |

---

### 5. Knowledge Base & Trend (RAG)

Konteks tren untuk membandingkan dengan pola editing yang sedang viral.

| Komponen | Status | Implementasi | File |
|----------|--------|--------------|------|
| **Real-time Google Trends** | ✅ Implemented | `trending.rs` — fetch trending topics regional (ID) + keyword interest scores | `src/analyze/trending.rs` |
| **Keyword-aware moment selection** | ✅ Implemented | Transkrip diformat dengan marker `[🎯 keyword]` untuk moments yang relevan dengan trending keywords | `src/analyze/service.rs` |
| **User-defined focus keywords** | ✅ Implemented | `--keywords` CLI arg, dikombinasikan dengan auto-extracted + trending keywords | `src/cli.rs` |
| **Vector Database** | ✅ Implemented | `rag/store.rs` — `RagStore` store/retrieve via sqlx + Supabase pgvector; embedding via `rag/embed.rs` (Gemini/Novita/OpenAI/vLLM); video_title & channel disimpan sebagai metadata | `src/rag/store.rs`, `src/rag/embed.rs` |
| **RAG similarity matching** | ✅ Implemented | `build_rag_context()` di analyze/service.rs — embed transcript → cosine similarity search → top-N similar moments diinjeksikan ke system prompt sebagai contoh | `src/analyze/service.rs` |
| **Editing style fingerprinting** | ✅ Implemented | `TrendAnalyzeService` — download trending videos → extract frames → vision LLM → `StyleProfile` JSON; simpan ke `style_profiles/` folder via `THOTH trend-analyze` | `src/analyze/trend_analyzer.rs` |

---

## Arsitektur Saat Ini

```
URL / File
    │
    ▼
[1. INGEST]  yt-dlp → video.mp4
                     ↓ (parallel)
           YT subtitles ─────────────────┐
                                         │
    ▼                                    │
[2. TRANSCRIBE]  Whisper CUDA / Groq API │
                 → transcript.json       │
                   (word timestamps)     │
                         │               │
    ▼                    ▼               │
[3. ANALYZE]  ┌── Text LLM ─────────────┘
              │   (Groq/Gemini/vLLM/etc)
              │   + Google Trends context
              │   → ViralMoment candidates (2× max_clips)
              │
              └── Vision LLM (optional)  ← frames dari video
                  (Gemini/Claude/vLLM)
                  → visual scores → re-rank → top N moments
                  → moments.json

    ▼
[4. EDIT]  Per clip:
           ├── Generate ASS subtitles (karaoke style)
           ├── Download TikTok overlay (optional, yt-dlp)
           ├── Resolve SFX from catalog (auto-discovery)
           ├── Resolve BGM from catalog (auto-discovery)
           └── FFmpeg encode:
               - Reframe (vertical/horizontal/square)
               - Subtitle burn
               - Headline lower-third panel
               - SFX + BGM mix
               - ClipStyle transition
               - TikTok overlay insert (optional)
               → clip_NNN.mp4
```

---

## Gap Prioritas (Roadmap)

### ✅ Priority 1 — Rich frame descriptions + combined prompt — **SELESAI**
**Diimplementasikan:** `describe_video_frames()` + `build_enriched_transcript()` + injeksi ke service

Untuk mengaktifkan:
```toml
[vision]
enabled        = true
describe_video = true       # ← aktifkan combined prompt
describe_interval = 10.0   # 1 frame per 10 detik
describe_batch    = 5      # 5 frame per API call
```

### ✅ Priority 2 — Scene boundary detection — **SELESAI**
**Diimplementasikan:** `detect_scene_boundaries()` + updated `extract_frames()` + scene-aware `describe_video_frames()`

Untuk mengaktifkan:
```toml
[vision]
scene_detection = true
scene_threshold = 0.3   # 0.0 = sangat sensitif, 1.0 = hard cuts only
```

### ✅ Priority 3 — Subtitle Styles (CapCut/WordPop/Minimal/Karaoke) — **SELESAI**
**Diimplementasikan:** `SubtitleStyle` enum + 4 gaya ASS + LLM picks per clip via `subtitle_style` field

### ✅ Priority 3 (lengkap) — Style Profiles + trend-analyze command — **SELESAI**
**Impact: Sangat Tinggi** — Langsung terlihat di hasil video, mengikuti tren editing terkini

#### Mengapa ini penting
Tren editing TikTok/Reels bergerak cepat. Tools yang hardcode gaya editing akan ketinggalan dalam 2 minggu. THOTH perlu sistem yang bisa di-update mengikuti tren tanpa harus recompile.

#### 3a. Style Profiles System

Named presets yang capture gaya editing trending. Disimpan di `config.toml` dan bisa di-update manual tiap bulan.

```toml
[style_profiles.tiktok_id_2025]
# Gaya TikTok Indonesia trending 2025
clip_style_default    = "flash"
subtitle_style        = "capcut_bold"   # kata tebal bergantian kuning/putih
overlay_behavior      = "sticker_corner"
sfx_on_hook           = true            # SFX tepat di kata pertama
bgm_duck_on_speech    = true
cut_pace              = "fast"          # avg 2-3s per segment

[style_profiles.youtube_shorts_edu]
clip_style_default    = "zoom"
subtitle_style        = "minimal_white"
overlay_behavior      = "pip_reaction"
sfx_on_hook           = false
bgm_duck_on_speech    = true
cut_pace              = "medium"
```

**Flow**: `content_category + energy + viral_type` → LLM picks best profile → apply to render

**Files to create/modify:**
- `src/config.rs` — `StyleProfile` struct + `profiles: HashMap<String, StyleProfile>`
- `config.toml` — default profiles
- `src/analyze/schema.rs` — add `style_profile: String` to `ViralMoment`
- `src/edit/service.rs` — apply profile parameters to `AudioOptions` per clip

#### 3b. CapCut-style Subtitle Animation

Ini yang paling langsung terlihat. Ganti subtitle karaoke biasa dengan animasi yang lebih dinamis:

**Saat ini**: kata highlight bergantian (kuning/putih), statik  
**Target**: kata muncul satu per satu dengan efek scale/bounce, background pill, warna dinamis berdasarkan energy

```
NORMAL word  ← putih, normal size
[HIGHLIGHT]  ← kuning, bold, scale 110%, background pill hitam
```

Untuk konten high-energy: kata muncul lebih cepat, warna lebih kontras  
Untuk konten emotional: transisi lebih halus, warna lebih soft

**Files to modify:**
- `src/edit/subtitle.rs` — add `SubtitleStyle` enum dengan variant `CapcutBold`, `MinimalWhite`, `HighEnergy`
- `src/edit/ffmpeg.rs` — build style-specific ASS filter

#### 3c. Reference Video Style Analyzer (New command)

```powershell
# Download & analyze 5 trending videos, buat style profile baru
THOTH trend-analyze \
  --hashtag "suratirta" \
  --sample 5 \
  --provider gemini \
  --output-profile tiktok_id_health_2025
```

**Proses**:
1. yt-dlp download N video dari TikTok hashtag/trending
2. Extract frames setiap 2s dari setiap video
3. Vision LLM analyze: "Describe editing style — caption, overlay, pacing, transitions"
4. Synthesize → generate style profile JSON
5. Save ke `style_profiles/` folder
6. Tersedia sebagai `--style-profile tiktok_id_health_2025` di run command

**Output style profile yang diekstrak:**
```json
{
  "avg_cut_every_secs": 2.3,
  "caption_style": "bold_yellow_words_white_bg",
  "overlay_common": "greenscreen_bottomright_35pct",
  "transition_dominant": "flash_white",
  "sfx_placement": "on_hook_and_peak",
  "bgm_energy": "upbeat_hiphop"
}
```

---

### ✅ Priority 4 — Beat-sync SFX/BGM — **SELESAI**
**Diimplementasikan:** BPM dari metadata + vibe fallback, SFX adelay ke downbeat, BGM ducking filter

Aktifkan dengan:
```toml
[assets]
beat_sync = true
```

### ✅ Priority 4 lanjutan — Beat-aligned ClipStyle transitions — **SELESAI**
**Diimplementasikan:** `clip_bpm` di AudioOptions, `fade_in_dur(bpm)` dan `fade_out_dur(bpm)` di ClipStyle, HeadlineAnim juga beat-aligned

Priority 4 sepenuhnya selesai:
- SFX snapped to downbeat ✅
- BGM ducking during speech ✅  
- ClipStyle transition duration = beat subdivision ✅ (Fade=1beat, Flash=½beat, Smooth=2beats)

### 🔵 Priority 5 — Vector DB + RAG
**Impact: Tinggi jangka panjang** — Memungkinkan "style matching" dengan video viral

Implementasi:
- Simpan `ViralMoment` + `VisualScore` ke Vector DB (e.g., Qdrant/Chroma)
- Embedding: encode editing parameters sebagai vector
- Saat analisis video baru: retrieve similar past viral clips → inject sebagai contoh ke prompt

### 🔵 Priority 6 — Full Adaptive Trend Learning
**Impact: Sangat Tinggi jangka panjang** — THOTH belajar sendiri dari tren terkini

Arsitektur lengkap yang perlu dibangun:

```
Sumber data tren (auto-pull):
├── Google Trends (sudah ada) → keywords viral
├── TikTok Creative Center   → trending sounds, hashtags
├── TikTok Hashtag pages     → format video dominan (via yt-dlp scraping)
└── YouTube Trending         → Shorts format/style

↓ Processing

Style Extractor:
├── Download 5-10 video trending per kategori
├── Vision LLM → extract style fingerprint per video
├── Synthesize → "gaya editing yang sedang dominan minggu ini"
└── Update style_profiles/ otomatis

↓ Apply

Render Engine (THOTH saat ini):
├── Pilih style profile yang paling sesuai + trending
├── Apply: transition, subtitle, overlay, sfx, bgm
└── Output clip yang mengikuti tren minggu ini
```

**Trend data sources yang bisa diakses gratis:**

| Sumber | Data | Cara akses |
|--------|------|-----------|
| Google Trends | Keywords | API (sudah ada) |
| TikTok hashtag | Format dominan | yt-dlp scraping |
| YouTube Trending | Shorts style | yt-dlp scraping |
| TikTok Creative Center | Sounds, hashtags | Web scraping |

**✅ SEBAGIAN TERIMPLEMENTASI (2026-06-27) — pendekatan DISKURSUS, bukan view-index:**
Alih-alih menarik index tren platform (TikTok Creative Center dst.) yang dikurasi algoritma, THOTH kini
memanen tren dari **apa yang warganet TULIS** di feed yang sudah ditemukannya sendiri. `scout/pulse_harvest.js`
(cron harian): scan `reel_topics.json` → scrape komentar berbudget → distilasi LLM term berulang (lintas
≥N video) + snapshot register gaya bahasa → simpan ke **CKB Supabase** `ckb_pulse` dengan recency-decay
(`exp(-age/τ)`) + TTL-prune. Disuntik ke narasi sbg `[Tren Diskursus]` (referensi gaya, bukan dipaksakan).
Detail desain: `RESEARCH_context_enrichment_narration.md` (Fase 3–4).
**Sisa Priority 6 yang BELUM:** auto-pull style-fingerprint VISUAL dari video trending (frame→vision→
update `style_profiles/` otomatis) — pulse saat ini fokus DISKURSUS/teks, belum gaya editing visual.

---

## News Enrichment Pipeline (Stage 4) — Status per Phase

Rencana lengkap: lihat `PLAN_REACT_NEWS_PIPELINE.md`. Pipeline baru menyisipkan
**Stage 4 (Enrich)** di antara Analyze (3) dan Edit (5). Prinsip utama: **semua
keyword pencarian berita berasal dari transcript yang diucapkan tiap moment**,
bukan dari field `title`/`reason` hasil paraphrase LLM.

| Komponen | Status | Implementasi | File |
|----------|--------|--------------|------|
| **NewsConfig + `[news]` section** | ✅ Done | Struct `NewsConfig` (provider, region, threshold, dll) + default + config.toml/.example | `src/config.rs`, `config.toml` |
| **Keyword extraction dari transcript** | ✅ Done | `extract_keywords()` — LLM baca `transcript_window` (via `words_starting_in`) → 3-5 query; parser toleran (JSON/fence/line fallback) + dedup | `src/news/keyword.rs` |
| **Internet search (Playwright)** | ✅ Done | Subprocess Python + Playwright (Bing News + fallback Google), 1 browser per moment (batched queries), tanpa API key | `src/news/search.rs`, `scripts/news_search.py` |
| **Internet search (Serper fallback)** | ✅ Done | `search_serper()` via `THOTH_SERPER_API_KEY` jika `provider = "serper"` | `src/news/search.rs` |
| **Dedup + relevance scoring + ranking** | ✅ Done | `dedup_by_url`, `score_relevance` (overlap 0.6 + freshness 0.25 + source 0.15), `within_max_age`, filter threshold, sort, truncate | `src/news/search.rs` |
| **EnrichService (orchestrasi Stage 4)** | ✅ Done | Sequential per moment, simpan `enrich/enrich.json`, best-effort (gagal tidak menggagalkan pipeline) | `src/news/service.rs`, `src/pipeline/mod.rs` |
| **EnrichResult di pipeline state** | ✅ Done | `StageResults.enrich: Option<EnrichResult>` (serde default, resumable) | `src/pipeline/state.rs` |
| **LLM provider factory (shared)** | ✅ Done | `build_llm_provider()` di-extract agar reusable oleh analyze + news | `src/analyze/provider/mod.rs` |
| **Conda env setup** | ✅ Done | `environment.yml`, `scripts/setup_THOTH_news.bat`, `conda_env` field di `NewsConfig`; command builder `util::python_command()` | `src/news/util.rs`, `config.rs` |
| **Headless screenshot berita (Phase 2)** | ✅ Done | `scraper::screenshot()` — subprocess `news_screenshot.py` (Playwright, popup cleanup JS, text extraction, graceful degrade); `scraper::ScreenshotResult` | `src/news/scraper.rs`, `scripts/news_screenshot.py` |
| **Screenshot 9:16 formatter (Phase 2)** | ✅ Done | `formatter::format_screenshot()` — FFmpeg `filter_complex` pillar-box (blur bg + scale overlay + drawtext caption source/date/headline); `escape_text`, `truncate_text` | `src/news/formatter.rs` |
| **News overlay di video (Phase 3)** | ✅ Done | `ImageOverlaySpec` (path, at_sec, dur, ken_burns) + `build_news_image_filter()` (FFmpeg fade-in/out + Ken Burns zoompan) + `enrich_data` loading + `audio_clone.news_overlay` per clip | `src/edit/ffmpeg.rs`, `src/edit/service.rs` |
| **Reaction script + TTS (Phase 4)** | ✅ Done | `src/reaction/` (model, script, tts, error, mod); LLM script generator; **5 TTS providers**: Edge (free), **MiniMax Speech 2.8 HD Sync** (recommended), **Fish Audio S2 Pro**, OpenAI, ElevenLabs; hex decode untuk MiniMax audio; binary streaming untuk Fish Audio; 3 unit tests decode_hex | `src/reaction/`, `src/config.rs` |
| **Static avatar + post-roll (Phase 5)** | ✅ Done | `src/reaction/avatar.rs`: `create_avatar_segment()` — FFmpeg bg (news screenshot / dark color) + optional PNG avatar PiP + TTS audio → MP4 segment; `concat_post_roll()` di `edit/ffmpeg.rs`; wired ke EnrichService (avatar segment) + EditService (post-roll concat) | `src/reaction/avatar.rs`, `src/edit/ffmpeg.rs`, `src/edit/service.rs` |
| **Local talking avatar — SadTalker (Phase 6)** | ✅ Done | `AvatarMode::SadTalker` di config; `create_sadtalker_segment()` di avatar.rs; `scripts/sadtalker_generate.py` wrapper SadTalker inference.py; `scripts/setup_sadtalker.bat` clone+install+model; conda env `THOTH-sadtalker` terpisah dari `THOTH-news` | `src/reaction/avatar.rs`, `src/config.rs`, `scripts/` |

**Cara mengaktifkan Phase 1:**
```toml
[news]
enabled = true
provider = "playwright"   # butuh: pip install playwright && python -m playwright install chromium
```
Output: `output/.THOTH/<job>/enrich/enrich.json` berisi `extracted_keywords` + `news[]` (URL + relevance_score) per moment.

---

## Catatan Teknis

### Provider yang Didukung

| Provider | Teks | Vision | Notes |
|----------|------|--------|-------|
| Groq | ✅ | ❌ | Free tier, 30K TPM (llama-3.3-70b) |
| OpenAI | ✅ | ✅ | gpt-4o-mini (vision: detail=low) |
| Claude | ✅ | ✅ | claude-sonnet-4-5 |
| Gemini | ✅ | ✅ | gemini-2.0-flash, free tier 15 RPM |
| vLLM | ✅ | ✅ | Self-hosted, mendukung reasoning models (gpt-oss-120b, qwen-vl) |
| Ollama | ✅ | ❌ | Local inference |

### Config File
```
config.toml      — semua konfigurasi (no secrets)
.env             — API keys (THOTH_*_API_KEY)
assets/          — semua media asset terpusat:
  ├ fonts/       — font files (Poppins auto-download, Montserrat-ExtraBold)
  ├ sfx/         — SFX files (auto-discovery by keyword)
  ├ bgm/         — BGM files (auto-discovery by keyword)
  ├ ui/          — grafis UI (arrow, kartu profil/komentar)
  ├ meme/        — template meme
  ├ asset_catalog.json — anotasi penempatan aset (5-beat) untuk LLM
  └ ASSET_CATALOG.md   — versi human-readable
footage_cache/   — Downloaded TikTok footage clips (cached by query hash)
models/          — Whisper GGML model files
output/          — Pipeline output (job artifacts)
```

### Struktur Output Per Job
```
output/.THOTH/{job_id}/
├── state.json           — pipeline state (resumable)
├── source/
│   └── {video_id}.mp4   — downloaded video
├── transcribe/
│   └── transcript.json  — word-level timestamps
├── analyze/
│   ├── moments.json     — ViralMoment list dengan visual scores
│   └── frames/          — temp frame files (deleted after vision analysis)
└── clips/
    ├── clip_000_*.ass   — ASS subtitle file
    └── clip_000_*.mp4   — final rendered clip
```

---

*Last updated: 2026-07-10 (hhh) — STANDAR CLIPPY REPO diselaraskan ke docs resmi (Configuration + Lint Configuration). Dasar: `cargo clippy --all-targets` via `rtk proxy` (hook rtk mem-filter cargo clippy jadi "No issues found" — WAJIB `rtk proxy cargo clippy ...` utk output nyata) menghasilkan 630 baris/591 warning; ditelusuri per lint code via `--message-format=json`. Ditemukan 2 lint volume-tinggi murni gaya (bukan bug) sefilosofi dgn allow cast_* yang sudah ada: `cast_lossless` (178 hit — konversi numerik WGPU/video/audio pakai `as`) dan `map_unwrap_or` (118 hit — gaya kontroversial, `.map().unwrap_or()` sudah jelas dibaca) → ditambah ke `[lints.clippy]` (Cargo.toml) sbg `allow` dgn komentar rasional, mengikuti pola existing. Lint domain-relevan (`float_cmp`, `case_sensitive_file_extension_comparisons`, `unused_async`, dll.) SENGAJA DIBIARKAN warn — proyek ini menganut "zero warnings **kritis**", bukan zero-semua-warning; `unwrap_used`/`todo` tetap warn (sudah berfungsi sesuai desain). Tambahan `.clippy.toml`: `avoid-breaking-exported-api = false` — thoth adalah binary-only crate (`[[bin]]`, tanpa `[lib]`), tak ada API publik lintas-crate yg perlu dijaga stabil, jadi clippy bisa kasih saran lebih presisi utk kode internal (unnecessary_wraps, needless_pass_by_ref_mut, dll.). Threshold existing (`too-many-arguments-threshold=8`, `cognitive-complexity-threshold=30`) dikonfirmasi masih relevan (7 fungsi >8 argumen tersisa — genuine complexity, dibiarkan utk cleanup lain). Verified: clippy 591→443 warning pada target bin (0 error tetap), `build_cuda.bat` EXIT=0 (0 err), thoth.exe mtime maju.*

*Last updated: 2026-07-09 (ggg) — SCOUT RUNTIME: HARD-SWITCH Node.js ≥24 → **Bun** (≥1.2). scout/ tetap TypeScript native tanpa build step, kini dijalankan via `bun cli.ts <perintah>`. Perubahan: (1) Rust `src/main.rs` handler `Commands::Scout` — `which::which("node")` → `which::which("bun")`, spawn `bun scout/cli.ts` (pesan error → https://bun.sh); `src/cli.rs` doc-comment `node`→`bun`. (2) 6 spawn internal TS `execFileSync('node', …)` → `execFileSync(process.execPath, …)` (child mewarisi runtime parent = bun, tanpa PATH lookup): run_pipeline/collect_comments/build_footage/topic_to_urls/trace_source (pipeline) + pulse_harvest (enrich); cli.ts sudah pakai process.execPath (tak diubah); browser.ts spawn binary browser (tak diubah). (3) `package.json` engines `{node:">=24"}` → `{bun:">=1.2"}`; `package-lock.json` (npm) DIHAPUS → `bun.lock` (di-commit); @types/node + typescript TETAP (tsc typecheck butuh tipe builtin `node:*`). (4) shebang cli.ts `env node`→`env bun`; semua string invokasi user-facing `node xxx.ts`→`bun xxx.ts` (usage msg, header comment, docs README/SETUP/RUNBOOK); `node:` IMPORT SPECIFIER dibiarkan (81 import — Bun dukung penuh). (5) docs: SETUP prasyarat "Node.js LTS"→"Bun ≥1.2", troubleshoot `node: command not found`→bun; CLAUDE.md kontrak scout + .gitignore comment. Verified: `bun run typecheck` (tsc --noEmit) 0 error; `bun cli.ts` list perintah OK; `bun lib/browser.ts status` (http/CDP path) jalan (DOWN = benar, browser belum start); `cargo check` + `build_cuda.bat` EXIT=0 (0 err/0 warn), thoth.exe mtime maju; end-to-end `thoth.exe scout` → spawn bun → cli.ts print list (rantai Rust→bun→ts terbukti live). Spec: `docs/superpowers/specs/2026-07-09-scout-node-to-bun-migration-design.md`.*

*Last updated: 2026-07-05 (fff) — AUDIT PENUH SEMUA OUTPUT CLI ke identitas "Feather Spine"/"Ink & Gold" (permintaan user: cek semua argumen/output pakai style yang sesuai). Ditemukan 2 celah: (1) Rust — command HANDLER (`main.rs`: Ingest/Transcribe/Analyze/Edit/Run/TrendAnalyze/Vocab/Thumbnail) tak pernah dipakaikan `brand.rs` (masih `println!` glyph mentah), dan top-level error path pakai default anyhow (tak berwarna). Fix: `brand.rs` BARU `stdout_color_enabled()`/`p_out()` (gate TTY terpisah utk stdout vs stderr) + `brand::ok/warn/err/field()`; semua handler di `main.rs` di-reroute; `main()`/`run()` di-restruktur utk styled error chain di top-level; hapus `println!("[BOOT]...")`; fix bug duplikat output di `Commands::Run` (`clips`→`_clips`, hapus println redundan — footer sudah dari `pipeline/mod.rs`). (2) scout (TS) — 24 file leaf-script (di luar orkestrator cli.ts/run_pipeline.ts/browser.ts yang sudah dibereskan pass (eee)) punya ~92 baris `console.log/error/warn` dgn glyph mentah (`✅❌⚠️`) tak lewat `ui.ts`. Fix: import `ui` ditambah ke 8 file yang belum punya; codemod data-driven (quote/paren-aware Node script) auto-transform 74+17 baris (2 ronde — ronde 2 nangkep `✅` bareplain yang lolos scan awal krn beda codepoint dari `✓`); 8 baris ternary/conditional-suffix (campur status+teks netral) di-tangan agar hanya glyph relevan yang diwarnai, bukan seluruh baris. Emoji dekoratif (📄💾📁📸🔗🔎🧵🎬⬇️) SENGAJA tak disentuh — bukan bagian identitas OK/WARN/ERR. Verified: `cargo check` 0 err, `build_cuda.bat` EXIT=0, live CLI test semua command Rust; scout `tsc --noEmit` 0 error, rescan akhir konfirmasi 0 baris status-glyph tersisa (22 sisa = semua dekoratif, dikonfirmasi manual).*

*Last updated: 2026-07-04 (eee) — LOG SCOUT (TS) DISAMAKAN ke identitas Rust. BARU `scout/lib/ui.ts` = mirror `src/brand.rs` (ANSI sama: gold `38;5;179`, violet `38;5;141`, cyan/amber/red/dim; glyph SPINE/BLOCK/🪶/✓/⚠/✗/·). Gating `process.stderr.isTTY && !NO_COLOR && TERM≠dumb` → plain text saat pipe. `ui.banner()` (`🪶 T H O T H · scout`), `ui.stage(label)` (`█ label`, TANPA uppercase ≠ Rust karena label scout = identifier lowercase), `ui.done(label,secs)`, `ui.rule()`/`ui.rule('thin')` (drop-in string pengganti box `'='`/`'-'.repeat(60)`). Wiring: `cli.ts` panggil `ui.banner()` sekali (entrypoint → tiap command buka sama); `run_pipeline.ts` pakai `ui.stage` untuk step header + RUN PIPELINE + RINGKASAN; 16 file lain `.repeat(60)` di-swap `ui.rule()` via codemod (import di-inject per kedalaman folder). Verified: `tsc --noEmit` 0 err, run piped `grep -c $'\\x1b'`=0, force-TTY render kode ANSI cocok dgn brand.rs. Box `═══` gaya lama (warisan openclaw) PENSIUN.*

*Last updated: 2026-07-04 (ddd) — LOG/OUTPUT RESTYLE Rust: identitas terminal tunggal **"Feather Spine" + palet "Ink & Gold"** (🪶 Thoth). BARU `src/brand.rs` = satu-satunya sumber palet (gold `38;5;179` brand/sukses, violet `38;5;141` spine/aksen, cyan info, amber warn, red error, dim chrome) + glyph (SPINE `▏`, BLOCK `█`, FEATHER 🪶, ✓/⚠/✗/·) + `banner()` (head log: `🪶 T H O T H` / `▏ <tagline dari Cargo desc> · v0.1.0`). Gating warna via `color_enabled()` (cached: stderr `IsTerminal` && !`NO_COLOR` && TERM≠dumb) → `p()` mengembalikan palet kosong saat non-TTY/pipe → output plain bersih tanpa ANSI. `log_style.rs::ThothFormatter` ditulis ulang: per-baris `▏ · msg` (info) / `▏ ⚠ msg` (warn amber) / `▏ ✗ msg` (error red) — buang prefix `🪶 Thoth |` tiap baris. `util/progress.rs`: `stage_header` → `█ LABEL   n/total · elapsed` (blok gold + label violet, elapsed dari OnceLock start-Instant), `stage_done` → `▏ ✓ label · Xs`, semua template indicatif diberi spine violet + spinner cyan. `main.rs`: `mod brand;` + `brand::banner()` sesudah subscriber init (ganti `info!("Thoth starting")` → debug). Hapus `src/test_fmt.rs` (duplikat mati, tak pernah di-`mod`). Verified: `cargo check` 0 err, `build_cuda.bat` EXIT=0 (release+cuda 1m12s, 0 err/0 warn), run nyata piped → `grep -c $'\\x1b'`=0 (NOL bocor ANSI saat non-TTY) + glyph spine render UTF-8 benar. Scout (TS) di luar cakupan pass ini. **Footer run** (`pipeline/mod.rs`): box `╔══╗` "Pipeline complete" gaya lama PENSIUN → `🪶 done · N clip(s) · Xs` (gold feather + violet count) lalu per-klip `▏ ✓ "title" (Ns) → path`; pakai `progress::elapsed_secs()` (accessor baru atas OnceLock start-Instant). Re-verified `build_cuda.bat` EXIT=0 (0 err/0 warn).*

*Last updated: 2026-07-04 (ccc) — SCOUT: REWRITE penuh ke **TypeScript** (45 file aktif, ~6.5k baris; lib/scrapers/pipeline/enrich/cli). Jalan **native via Node ≥24 type stripping — TANPA build step**: `node cli.ts <perintah>`. Perubahan: (1) semua `.js` aktif → `.ts` ESM (require→import, module.exports→export, __dirname→import.meta.dirname, require.main→import.meta.main); `scout/package.json` BARU (`type:module`, devDeps typescript/@types/node untuk `npm run typecheck` = `tsc --noEmit`, optionalDeps pg) + `tsconfig.json` (nodenext, erasableSyntaxOnly, allowImportingTsExtensions; strict off — boundary CDP/LLM tak bertipe, naikkan bertahap); `deprecated/package.json` (`type:commonjs`) agar file lama tetap parseable. (2) `lib/types.ts` BARU — kontrak ContentSet/MainVideo/ContentResult/CommentInfo (mirror struct Rust content_search.rs), dipakai lint `lib/validate.ts`; `lib/cdp.ts` diketik penuh (CdpClient/ConnectOpts/CdpTarget). (3) Bugfix nyata ketemu saat konversi: `pulse_harvest` print `ckb.CKB_PATH` yang tak pernah diekspor (undefined sejak dulu) → ekspor `LOCAL_PATH`. (4) Lazy require → dynamic import (pg di ckb, web_grounding di enrich_context, tiktok_profile di profile_crop, cdp di tiktok_video); semua spawn antar-script → nama `.ts`. Verified: `tsc --noEmit` 0 error; dynamic-import 19 modul lib OK; `cli.ts validate` PASS content-set nyata; browser status UP; LIVE LLM Novita via env.ts OK; CKB Supabase live via `import('pg')` OK (16 entities/18 memes); 16 CLI spawnable print usage. `.js` aktif DIHAPUS (deprecated/ tetap .js). ⚠️ FOLLOW-UP: `run_full.ps1` masih memanggil path `.js` (user minta diabaikan dulu) — perlu ganti ke `cli.ts`/`.ts` sebelum dipakai lagi. Docs scout README/SETUP/RUNBOOK + CLAUDE.md diperbarui.*

*Last updated: 2026-07-04 (bbb) — SCOUT: credential terpusat `.env` root + entrypoint tunggal `cli.js` (persiapan rewrite TypeScript). (1) `scout/lib/env.js` BARU: parse `.env` ROOT repo (file yang sama dipakai Thoth Rust) sekali saat require → inject `process.env` (shell env menang); getter `novitaKey()/groqKey()/supabaseUrl()/get()`. (2) 10 file aktif dipatch berhenti baca key file per-folder (`.novita_key`/`.groq_key`/`.supabase_url` — nilai diverifikasi identik dgn `.env` via hash sebelum switch): embed/footage_objects (lib), resolve_source/extract_figures/discover_reels/trace_source (pipeline), enrich_context/pulse_harvest/ckb (enrich; readEnvVar+supabaseUrl ckb.js diganti env.js), vision_crop (scrapers). Key file lama masih di disk tapi TAK dibaca script aktif (hanya deprecated/) — boleh dihapus. (3) `scout/cli.js` BARU — entrypoint tunggal `node cli.js <perintah> [args]` (browser/discover/trending/run/comments/footage/figures/enrich/images/validate/pulse/topics/news), dispatch spawnSync + forward args & exit code; tanpa argumen = daftar perintah. Verified: node --check 12 file, `cli.js validate` PASS content-set nyata, `cli.js browser status` UP, LIVE LLM call (footage_objects) pakai key dari .env → sukses. Docs: scout/README.md + SETUP.md (§credential → .env root, contoh perintah → cli.js). Tanpa perubahan Rust.*

*Last updated: 2026-07-03 (aaa) — RENAME openclaw→scout + COLLAPSE. Folder sourcing JS `openclaw/` di-`git mv` ke **`scout/`**; runtime di-collapse: tak ada lagi `~/.openclaw/workspace` terpisah maupun `sync.js` (dihapus) — script dijalankan langsung dari `CLIPPER/scout/` (`lib/paths.js::WORKSPACE` kini = folder scout/ itu sendiri, __dirname-based). "Ella"/agent Telegram pensiun. Rust: struct `OpenClawContentSet`→`ContentSet` (`ingest/content_search.rs`) + semua komentar/log string openclaw→scout di 11 file src (cargo check + build_cuda PASS, 0 error/warn). Docs dipurge menyeluruh: CLAUDE.md (kontrak content-set), README/SETUP root, scout/README+SETUP+RUNBOOK (SETUP di-rewrite: managed browser + key di scout/, buang panduan install openclaw lama), docs/MODELS.md, .env.example, CHANGELOG.md, skills/skill-creator (drop half Ella), run_full.ps1, .gitignore. `grep -riE openclaw` seluruh repo = 0. Env fallback `OPENCLAW_CDP` di cdp.js dihapus (tinggal `THOTH_CDP`||18800). Terkait entri (zz) di bawah.*

*Last updated: 2026-07-03 (zz) — BROWSER CDP STANDALONE: `scout/lib/browser.js` (Node builtins only) melaunch Brave/Chrome/Edge dengan `--remote-debugging-port=18800 --remote-allow-origins=* --user-data-dir=~/.clipper/browser-profile` + hardening flags → menyajikan CDP standar (`/json/version` ready, `/json` targets, WS=raw CDP) yang `lib/cdp.js` sudah bicara. CLI `start|status|stop|path|env`; auto-detect Brave>Chrome>Edge (override `THOTH_BROWSER_BIN`); login TikTok/IG/X sekali → persisten (tanpa extension/pihak-ketiga). `lib/cdp.js` default endpoint `THOTH_CDP||http://127.0.0.1:18800`. Verified LIVE (bukan syntax): start→CDP siap 18800 (Brave pid nyata)→listTargets→connect+Runtime.evaluate+Page.navigate example.com→status UP→stop→DOWN. Env: THOTH_CDP_PORT/BROWSER_BIN/BROWSER_PROFILE/BROWSER_HEADLESS.*
*Last updated: 2026-07-03 (yy) — RESTRUKTURISASI MODULAR scout/ + scripts/ (permintaan user). scout/ dari 45 file JS flat → subfolder: `lib/` (9 shared: cdp/paths/verify/validate/embed/comments/comment_engine/aggregators/footage_objects), `scrapers/` (17 per-platform), `pipeline/` (13 tahapan content-set), `enrich/` (4: enrich_context/web_grounding/pulse_harvest/ckb), `config/` (ig_accounts.json+curator_accounts.json), test_narration→deprecated/. Workspace `scout/` di-mirror 1:1 (sync.js rewrite rekursif); file flat lama workspace di-backup ke `_backup_flat_20260703/`. KUNCI: `lib/paths.js::WORKSPACE = resolve(__dirname,'..')` jadi satu-satunya resolusi root — semua pembaca key file (`.novita_key` dll 10 file), config JSON (aggregators/discover_reels/discover_topics), ckb.json/.supabase_url/.env (ckb.js), dan spawn lintas-folder (collect_comments+pulse_harvest→`../scrapers/`, topic_to_urls here()→`../scrapers/`, run_pipeline step enrich_context→`../enrich/`) diganti `__dirname,'..'`. Referensi eksternal diupdate: skills scout (content-sourcing, trend-scout), TOOLS.md/AGENTS.md/MEMORY.md workspace, run_full.ps1, CLAUDE.md, scout README/RUNBOOK/SETUP. scripts/ Python 14 file → `narration/` `news/` (termasuk social_search.py) `tts/` `render/` `media/`; 4 file ROOT `Path(__file__).parent.parent` → tambah satu `.parent`; path literal Rust diupdate (config.rs defaults ×5, edit/service.rs ×2; tts_narrate.py resolusi sibling tts_script → tetap) + config.toml/.example. Rust TIDAK memanggil JS scout (dikonfirmasi) → restrukturisasi JS tanpa dampak Rust. Verifikasi: node --check 49 file + resolver require 0 broken + smoke workspace (paths.WORKSPACE benar, 8 curated handles, CLI entries jalan), cargo check 0 err, build_cuda.bat EXIT=0 (1m18s), 60 edit tests PASS. Konvensi baru: panggil `node pipeline/xxx.js` / `node scrapers/xxx.js` dari workspace root.*
*Last updated: 2026-07-02 (xx) — DISCOVERY SHAPE-AWARE + MULTI-PLATFORM (PLAN_discover_shape_aware_multiplatform.md). Primitif baru `verify.js::postShape(url)`: SATU yt-dlp -J → {shape video|photo|carousel, slides[kind/duration], caption, time} — platform-agnostic (IG/TikTok/X/FB/YT), cache per-run, `--ignore-no-formats-error` (foto tak error). discover_reels: tangga topik per bentuk (video→audio→caption; photo→caption; carousel→caption→audio slide-video `--playlist-items N`), ganti heuristik DOM `<video>` yang false-positive di carousel campur; field `shape` di reel_topics.json; `audioTopic(url, slideIndex)`. Kurator multi-platform: `curator_accounts.json` {instagram,tiktok,x} (ig_accounts.json tetap di-merge; flags --tiktok-accounts/--x-accounts), loop TikTok reuse tiktokProfileVideos (views NYATA → scoring; recency dari postShape.time krn grid TikTok tanpa <time>), loop X pakai scraper BARU `x_profile.js::xProfileTweets` (CDP; tweet teks = topik langsung). Verified live: 2 post gagal @jktlogy kini via=caption; YouTube probe lintas-platform OK. JS-only (tanpa rebuild); sync 4 file.*
*Last updated: 2026-07-02 (ww) — MODEL VISION DEPRECATED NOVITA (root cause discover "(tak terbaca)" massal). Novita menghapus `qwen/qwen3-vl-8b-instruct` + `qwen/qwen2.5-vl-72b-instruct` (500 MODEL_NOT_AVAILABLE; masih ke-listing di katalog dgn status=4) → visionHook & Rust `describe_video` gagal DIAM-DIAM (non-OK ditelan jadi ''). Fix (PLAN_discover_topics_vision_fix.md, 5 fase): (1) default model → `qwen3-vl-30b-a3b-instruct` (5 file JS: discover_reels/comments/vision_crop/trace_source/resolve_source) + `[vision] novita_model` → `qwen3-vl-235b-a22b-instruct` (config.toml/.example, config.rs:1705, MODELS.md, .env.example) — dipilih via probe live + head-to-head OCR (30b sempurna; deepseek-ocr halusinasi; paddleocr-vl tak respons chat endpoint). (2) visionHook log 1x/run bila API non-OK/error (anti-silent-fail). (3) audioTopic pakai `verify.js::ytdlpCookieArgs()` (YTDLP_COOKIES_FILE dari run_full — export baru di verify.js) → audio fallback IG hidup, bukan selalu "login-wall". (4) scoreOf: `(v||1)` saat DECAY_ON + fmtScore 3 desimal — half-life tetap mengurutkan walau grid IG views=0 semua (bukan `score 0` konstan). (5) collectItems: 1x scroll+retry bila snapshot grid kosong (kasus @jktlogy). Build: build_cuda.bat EXIT=0 0 err; sync.js push 6 file. ⚠️ Background-task PowerShell cwd ≠ repo → `.\build_cuda.bat` silent-fail exit 1 dgn build_log BASI — jalankan foreground/Set-Location dulu.*
*Last updated: 2026-06-08 (vv) — HAPUS auto-search overlay (footage HANYA dari scout). User: footage sudah dikurasi scout → buang fitur THOTH cari footage sendiri. Dihapus: `fetch_overlay_clip` (query-search + scraper + fallback YouTube) di `src/edit/overlay.rs`, call-fallback di `src/edit/service.rs` (event path: kalau pool scout kosong/gagal → overlay di-SKIP, tak ngarang), seluruh modul `src/scraper/` (+`mod scraper;` main.rs) krn cuma dipakai fetch_overlay_clip (news punya scraper terpisah). TETAP: `fetch_overlay_from_url` (download URL footage scout → kartu video) + image-card crop. Config `[overlay] scraper_enabled/fallback_to_youtube/max_variants` jadi tak terpakai (pub field, tak warning). Build: cargo check + 86 test PASS (−4 test ikut modul scraper) + build_cuda.bat PASS (release+cuda 1m12s, 0 err/warn).*
*Last updated: 2026-06-08 (uu) — ROADMAP enrich content-set, THOTH side (Rust) SELESAI. (A) `ContentResult.description` + struct `Figure` + `figures[]` di scoutContentSet/LoadedSet/MainContext (`src/ingest/content_search.rs`, serde-default). (B) figures ditulis ke `content_context.json` (main.rs) + blok `[Tokoh]` di grounding narasi (`src/pipeline/mod.rs::generate_narration`). (C) EMBEDDING-PLACEMENT footage di `src/edit/service.rs::render_narration_video`: ganti penempatan ROUND-ROBIN → tiap window montase ambil teks narasi (word timings `narration_words()`, digeser `lead`) → `embed_text_with_config` (EmbedConfig::from_app_config) → cosine vs `footage.description` → footage paling cocok ditempatkan di window itu; fallback round-robin bila embed invalid/desc kosong (helper `narration_window_text`+`cosine`). Build: cargo check + 90 unit test PASS + build_cuda.bat PASS (release+cuda 56s, 0 err/warn). ⚠️ build_cuda.bat harus via PowerShell full-path (bukan Bash cmd/c relatif).*
*Last updated: 2026-06-08 (tt) — ROADMAP enrich content-set, scout side SELESAI (THOTH side menyusul). (1) footage kini bawa `description` (build_footage: video=caption oEmbed, post=teks crop) — utk embedding-placement. (2) `collect_comments.js`: isi comments[] dari banyak sumber (post main + footage relevan + --extra post IG asal), dispatch scraper per-platform (crop+data), merge/dedupe/cap; + FIX scrape_comments_ig reel (klik ikon Comment dulu krn /reels/ viewer sembunyikan komentar) — verified reel ojol→5 komentar+crop. (3) `extract_figures.js` (figuresFrom): LLM ekstrak tokoh→set.figures[{name,type:person|organization|community,role,description}], [] kalau orang biasa — verified ojol→[], Silmy Karim→person, Bahlil→person+org HIPMI. NEXT (Rust, build_cuda.bat): ContentResult.description + top-level figures[] (intro card/grounding) + EMBEDDING-PLACEMENT footage (ganti round-robin service.rs:1343 → cocokkan description footage↔segmen narasi via cosine). Tanpa perubahan Rust SOFAR.*
*Last updated: 2026-06-08 (ss) — FOOTAGE query = OBJEK VISUAL dari teks (`footage_objects.js`, scout). User: query footage harus objek yg diekstrak dari description+caption+headline (bukan query topik). `footageObjects({description,caption,headline})` → LLM ekstrak objek visual b-roll + EKSPANSI BRAND otomatis ("ojol"→gojek,grab,maxim; "mobil sport"→porsche), buang objek abstrak/nama orang. Butuh model kuat → default qwen3-vl-235b (8b malformed). Live-verified topik ojol → ["ojek online","gojek","grab","indofest senayan","cartek manual","motor ojol"]. + `build_footage.js <content_set> [--objects][--per 2][--max 3]`: tiap objek di-search (gated) → ambil campuran VIDEO b-roll (tiktok/youtube) + POST (x/ig/fb/threads→cropPost image_path), footage relevance:"match"+query=objek, dedupe. Live-verified objek "gojek" → tiktok b-roll + tweet "naik Gojek…'lima puluh delapan'" (persis cerita ojol). Tanpa perubahan Rust.*
*Last updated: 2026-06-08 (rr) — Threads: pilih post by KEYWORD + download mp4 lokal (lanjutan qq). (1) `findOriginalThreads(username,keywords)` kini baca teks tiap post di profil (utamakan post milik user `/@user/post/`) → PILIH yang match keyword (any), bukan terbaru — live-verified pilih DZPjUSdGcWG (ojol) dari [ojol,Indofest,Senayan]. (2) `threads_video.downloadThreads(vurl,out)` + `trace_source` simpan `main.source_local=output/threads_<code>.mp4` (cadangan krn fbcdn ephemeral; `--no-threads-dl` skip). End-to-end verified: reel desc "Threads oleh @sashariella" → resolveSource {sashariella,threads} → post match-keyword → fbcdn video (mp4 663KB ter-download) → main(is_video:true,url=fbcdn,source_url=post,source_local=mp4). Tanpa perubahan Rust.*
*Last updated: 2026-06-08 (qq) — VIDEO THREADS BISA DI-DOWNLOAD (koreksi pp) + handle video nested. User: "video di dalam postingan lagi" (post Threads quote/embed post lain berisi video). Temuan: yt-dlp tolak PAGE threads.com TAPI `<video>.currentSrc` = URL Meta-CDN langsung (`instagram.*.fbcdn.net`→`scontent.cdninstagram.com/*.mp4`) yang yt-dlp/fetch BISA download (live-verified mp4 663KB). Baru: `threads_video.js` (`threadsVideoSrc(postUrl)`) — CDP buka post → ambil `<video>` TERBESAR (tangani video nested) → URL fbcdn; CLI `--download`. `trace_source` branch threads kini: findOriginalThreads→post→threadsVideoSrc→fbcdn → GANTI main (is_video:true, url=fbcdn, source_url=post) bila ada video; post teks → catat image-card. ⚠️ URL fbcdn SIGNED & EPHEMERAL (jam-an) → THOTH harus jalan segera. Threads DOM: `data-pressable-container="true"` (crop), profil→`a[href*="/post/"]`. Tanpa perubahan Rust.*
*Last updated: 2026-06-08 (pp) — THREADS ditambah sbg platform sumber (scout). Banyak reel kurator merujuk sumber ke Threads (mis. reel IG DZRlKf3TGWt → caption "unggahan di Threads oleh @sashariella"). Ditambah: `resolve_source` PLATFORMS +threads (LLM kembalikan platform "threads" — live-verified @sashariella→{threads}); `crop_post` inferPlatform+adapter threads (match threads.com/.net, idRe /post/<code>); `trace_source` handleMatch threads + `findOriginalThreads` (profil threads.com/@user→post terbaru, guarded butuh tab threads) + branch platHint='threads' + rewrap_platform; `topic_to_urls` canon + `urls_to_contentset` platOf +threads. Recognizer (inferPlatform/handle/platform-vocab) unit-tested. **LIVE-VERIFIED** (tab threads): Threads pakai `data-pressable-container="true"` (bukan role=article); crop_post Threads crop BERSIH (638x915, header+teks+frame video+actions); profil→`a[href*="/post/"]` OK. **TEMUAN: yt-dlp TIDAK support Threads** ("Unsupported URL") → video Threads tak bisa di-download → Threads HANYA jadi KARTU GAMBAR (crop, is_video:false), BUKAN main video. `trace_source` branch threads kini hanya CATAT sumber (rewrap_source/platform/url) + main dibiarkan (tak bikin main non-video rusak). Tanpa perubahan Rust.*
*Last updated: 2026-06-08 (oo) — DISCOVERY REELS akun kurator + RESOLVE SUMBER MAIN by LLM (scout). (1) `discover_reels.js`: topik dari reels akun kurator IG (sadampermana.w/jktlogy/basevox/unexplnd) — topiknya di voiceover/headline bukan caption (caption cuma nama lagu); ambil reel ≤48 jam + view count → vision baca teks HOOK frame-1 = angle (live-verified: "Porsche disita rumah Silmy Karim", "Istri cerita suami ojol ongkos 58"); audio Whisper (Groq) GUARDED (no GROQ key skrg → di-skip, vision cukup); rank views+recency. Sering memunculkan angle viral lebih dulu dari trends X/YT. (2) `resolve_source.js` (modul `resolveSource`): LLM Novita baca description+caption+headline → `{source:{account,platform}|null, keywords[]}` (ada kredit→source; tidak→keywords spesifik). `trace_source.js` detektor di-upgrade ke LLM ini: kumpulkan desc+caption(oEmbed)+headline(vision baca teks cover) → resolveSource → source→cari & GANTI main (handle/channel-match); keywords→simpan `main.source_keywords`, kalau main NON-VIDEO search keyword→video jadi main (main video bagus tak diganti). Live-verified. Tanpa perubahan Rust. Docs: scout `TOOLS.md`, memory.*
*Last updated: 2026-06-12 (pp) — FIX BUG narasi gagal parse JSON krn LLM pakai key alternatif (lanjutan test end-to-end optimal flow, topik "Damkar Padang dihadang mobil"). Run pertama: `narration/mod.rs::generate_script` gagal `parse narration JSON: missing field 'narration'` krn LLM (deepseek-v4-flash) balas `{"hook":..., "narrator": "..."}` (key `narrator` bukan `narration`) — narasi yg sebenarnya SUDAH BAGUS & tergrounded benar ("Bayangin, lo lagi ngebut mau padamin kebakaran...") tapi gagal di-parse → silent fallback ke clip-mode → video jadi tentang lirik lagu "Cinta dan Tujaku" (viral-moment salah, dari audio asli yg cuma musik trending) bukan cerita Damkar. FIX: struct `Out` di `generate_script` +`#[serde(alias = "narrator", alias = "script", alias = "naskah")]` pada field `narration` — toleransi key alternatif yg kadang dipakai LLM. Rebuild `build_cuda.bat` OK (53s, 0 error). Re-run content-set SAMA (`damkar_padang.json`, FOOTAGE=7, FIGURES=1, COMMENTS=6 dgn crop asli pertama kali sukses) → narasi sukses ("Satu aksi petugas Damkar yang bikin geleng-geleng:", 89 kata, narrator-driven 35.3s) + 1 footage match + 3 comment card (crop screenshot ASLI dari IG, semua on-topic re: video larangan lawan-arah/mobil modif). QC blackdetect bersih, 7 frame sample (hook+footage+3 comment card) semua relevan & tak ada cacat visual. Full optimal flow (`discover_reels → run_pipeline → THOTH run`) kini SUKSES end-to-end DUA topik berbeda berturut-turut, comment-crop pipeline pertama kali menghasilkan kartu nyata (bukan 0). Tanpa perubahan config/schema baru — pure bugfix `narration/mod.rs` (1 baris attribute serde).*
*Last updated: 2026-06-12 (oo) — FIX BUG sidecar basi (`content_comments.json`/`content_profile.json`) + verifikasi end-to-end flow OPTIMAL (`discover_reels.js → run_pipeline.js → THOTH run`). Konteks: `--provider` default sudah `novita` (dipindah dari `groq` di sesi sebelumnya, `src/cli.rs` `AnalyzeArgs`+`RunArgs`) — provider groq kena rate-limit 12k TPM bikin narasi diam-diam gagal → fallback clip-mode; novita confirmed re-test ini (narasi "Pencuri helm di Denpasar punya stok kayak toko—serius?", 82 kata, narrator-driven). BUG ditemukan: `main.rs` cuma menulis sidecar `content_comments.json`/`content_profile.json` KONDISIONAL (`if !set.comments.is_empty()` / `if let Some(p) = &set.profile`) ke `output/` BERSAMA — kalau content-set run berikutnya `comments:[]`/tanpa `profile`, sidecar run SEBELUMNYA tetap dipakai edit stage → komentar/profil topik LAMA nyasar ke video topik BARU (terbukti dari QC frame: comment card "sopir angkot"/"rezeki dan derajat" muncul di video helm/AirTag). FIX: `main.rs` `std::fs::remove_file` KEDUA sidecar TANPA KONDISI sebelum blok tulis kondisional masing-masing (clear-then-maybe-write). Rebuild `build_cuda.bat` OK (1m06s, 0 error). Re-run `THOTH run --content helm_airtag.json` → log TANPA `"💬 scout comment pool"` (sidecar basi sukses dibersihkan) + 1 footage (AirTag tracker) ter-placement. QC frame klip kedua (3s/10s/30s) bersih: tak ada comment card nyasar, tak ada black frame, footage AirTag nyambung dgn cerita. Full flow optimal (bukan `run_topic.js` deprecated) terverifikasi end-to-end pertama kali: `discover_reels.js` (topik dari hook on-screen IG, akun `jktlogy`) → `run_pipeline.js` (FOOTAGE=6, FIGURES=2, COMMENTS=0 — `scrape_comments_ig.js` 0 hasil utk reel ini, known limitation DOM-selector, BELUM diperbaiki) → `THOTH run --content` narrator-driven 35.3s. Tanpa perubahan config/schema baru — pure bugfix `main.rs`.*
*Last updated: 2026-06-07 (nn) — 2 perbaikan scout: (1) `topic_to_urls.js` RETRY-ON-ZERO — kalau sebuah platform balik 0 URL (hiccup transien; pernah TikTok+X 0 padahal standalone ada 36 video → main jatuh ke IG non-video), retry 1× (jeda 2.5s) sebelum menyerah. (2) `discover_topics.js` default EXCLUDE topik SEPAK BOLA (`FOOTBALL_RE`: term football sbg substring biar tangkap hashtag gabung "#PialaAFF2026", + `\bvs\b`/`\baff\b`/`\bgol\b` bounded biar "golf"/"affair" aman; tangkap juga "Indonesia Vs Vietnam"; `--include-football` override). Live-verified: discover X "✅ 13 (2 sepak bola di-skip)". Tanpa perubahan Rust. Docs: scout `TOOLS.md`, memory.*
*Last updated: 2026-06-07 (mm) — FIX crop X (Twitter) kepotong kiri (`crop_post.js`). Gejala: crop tweet TINGGI (ada media/quoted card) teksnya kepotong ~2-3 huruf di kiri. Root cause: `captureBeyondViewport` me-render halaman ~40px geser horizontal vs `getBoundingClientRect` pada elemen tinggi (X viewport ~2519px lebar, re-center) — shift tergantung TINGGI (elemen pendek aman → komentar/news card tak kena; post bisa tinggi → kena). FIX: kalau elemen MUAT di viewport → screenshot viewport biasa (tanpa clip/beyondViewport) + crop lokal via ffmpeg (WYSIWYG); kalau lebih tinggi → fallback captureBeyondViewport (andal di IG/FB/YT). Elemen di-scroll ke `top-64` (lewati sticky bar). `comment_engine`/`search_news` tak diubah (elemen pendek). VERIFIED: 3 tweet (FirzaHusain/ahmad_bellamy/Anak_Ogi) + IG semua bersih penuh, validate PASS. Tanpa perubahan Rust.*
*Last updated: 2026-06-07 (ll) — ANTI RE-WRAP: cari sumber asli lintas platform diperbaiki (lanjutan kk, dari test "Bahlil HIPMI"). Sumber asli `hipmi_tv` ternyata channel YOUTUBE "HIPMI TV" (bukan TikTok/X). Fix `trace_source.js`: (1) `findOriginalYouTube` — search YouTube `"<username words> <topik>"` (mis. "hipmi tv bahlil") lalu cocokkan CHANNEL NAME (watch URL tak punya handle), dijalankan SEBELUM pencarian TikTok/X. (2) guard "abaikan source vision yg memuat keyword topik" DIHAPUS — salah, channel sumber sering dinamai subjek ("HIPMI TV" meliput HIPMI); validator sebenarnya = handle/channel-match. (3) `normHandle` strip @+spasi+`._-` → "HIPMI TV"="hipmi_tv"="hipmitv". (4) channel YT innerText sering DOBEL → ambil baris pertama. VERIFIED live: main re-wrap @totalpolitik (TikTok, ada headline+watermark) → diganti ke sumber asli YouTube HIPMI TV `watch?v=8T_VTylNB0Q` ("Sambutan Menteri Investasi…Bahlil"), is_video true, source_traced=hipmi_tv, validate PASS. Tanpa perubahan Rust.*
*Last updated: 2026-06-07 (kk) — ANTI RE-WRAP main (scout `trace_source.js`). Masalah: main kadang REPOST akun lain (sudah ada headline text + watermark) → THOTH nimpa headline sendiri → dobel/menyampah. Solusi (MAIN saja; caption-dulu-lalu-vision; fallback keep+flag): (1) regex kredit caption (cr/source/via/ig:/@…/emoji) → username; (2) kalau tak ada → VISION cover (oEmbed thumbnail) baca watermark/sumber (Novita); (3) username → cari ASLI "<username> <topik>" lintas socmed (reuse topic_to_urls) → GANTI main HANYA dgn post di handle itu (TikTok/X handle-match, utamakan video); tak ketemu → `main.rewrap=true`+hint, main dipertahankan (THOTH abaikan field unknown). GOTCHA yg diperbaiki saat test: (a) JANGAN ganti main dgn video acak kalau handle tak match (dulu salah ganti totalpolitik→retorika.show) → hanya ganti kalau handle-match; (b) vision 8b sering baca branding SUBJEK sbg sumber (cover Bahlil/HIPMI → "hipmi_tv") → guard: source vision yg mengandung keyword TOPIK diabaikan. `--username` paksa sumber. Wired ke `run_topic.js` step 2a (`--no-trace` lewati). Tested: regex unit lulus; auto pada totalpolitik → guard benar (main dibiarkan, no false-flag); forced --username → handle-match replace terbukti. Tanpa perubahan Rust. Docs: scout `TOOLS.md`, memory.*
*Last updated: 2026-06-07 (jj) — 3 FIX dari FULL-PIPELINE TEST (scout, topik "Bahlil HIPMI"). Test end-to-end ungkap output cacat → diperbaiki: (1) MAIN tidak di-gate → main jadi TikTok spam ("Griffin Method"), bukan on-topic; FIX: `urls_to_contentset` pilih main = video pertama yg caption oEmbed match keyword (mode any), fallback+warning. (2) TikTok search tak di-gate di sumber → spam masuk; FIX: `topic_to_urls` teruskan keyword primer ke `search_tiktok_v2` (cukup 1 entitas krn gate ALL-mode; relevansi halus di-recheck downstream mode any). (3) footage non-video gagal crop (tab lepas) → is_video:false TANPA image_path → validate FAIL (1 gagal transien gagalkan seluruh pipeline); FIX: `enrich_image_paths` buang footage non-video tanpa image_path (attach tab+--force utk pulihkan). After-fix VERIFIED: main "Cerita Bahlil saat maju Ketum HIPMI" (@totalpolitik) on-topic, footage match=11 (tiktok2+tw3+ig3+news3), 5 komentar viral (10.1rb❤), validate PASS errors=0 warnings=0. Pipeline scout kini matang ujung-ke-ujung. Docs: scout scripts + memory. Tanpa perubahan Rust.*
*Last updated: 2026-06-07 (ii) — NEWS PINTAR PINDAH KE SCOUT (`search_news.js`), dulu di THOTH (`src/news/`). Via CDP/Google, AUTO-detect: topik MATA UANG (regex term currency, mis. "rupiah menurun dibanding dolar") → query Google "USD to IDR" → crop widget KURS+CHART jadi image card + nilai ("18.095,70 Rupiah"); selain itu → Google News (`tbm=nws`) → crop kartu artikel teratas (title/url/source). Anchor chart STRUKTURAL (anti obfuscation Google): panjat dari `<select>` ke ancestor ber-2-select+svg, +1 parent utk kartu penuh (~652x353); capture page-coords+captureBeyondViewport+blank-guard. `--append <content_set>` melipat hasil ke `footage[]` sbg image card siap (is_video:false, image_path, relevance:"match"). Wired ke `run_topic.js` (step 2b, sebelum enrich; `--no-news` lewati). Butuh tab google.com (fallback: tab apa pun→navigate Google, no-login). Live-verified: chart USD/IDR crop sempurna (grafik rupiah melemah); articles "BPJPH" 4 kartu ter-crop+ter-append, validate PASS. Rust `src/news/` tetap utk run `--url`; jalur content-set kini pakai search_news. Pipeline scout final: discover_topics → run_topic (topic_to_urls→urls_to_contentset→search_news→enrich→validate) → THOTH run. Docs: scout `TOOLS.md`, memory. Tanpa perubahan Rust.*
*Last updated: 2026-06-07 (hh) — GATE RELEVANSI + WRAPPER SATU-PERINTAH (scout). Temuan saat test wrapper: THOTH `content_search.rs` (enrichment loader) MEMBUANG footage `relevance != "match"` (`unverified` di-drop), jadi content-set tanpa gate = semua footage terbuang. Fix gate berbasis caption asli (keyword default=topik, mode "any"): (1) VIDEO footage di-gate di `urls_to_contentset.js` via public oEmbed (`verify.tiktokOembed` + `youtubeOembed` BARU) → match/unverified; (2) NON-VIDEO di-gate di `enrich_image_paths.js --keywords` — `crop_post.cropPost` kini return `text` (innerText post saat crop) → `matchesTopic` → match/unverified, tanpa kunjungan ekstra; (3) wrapper `run_topic.js "<topik>"` = SATU perintah seluruh hulu (topic_to_urls→urls_to_contentset→enrich→validate, cetak perintah THOTH; THOTH TIDAK auto-run). Live "BPJPH": video 0 match (TikTok spam off-topic, benar di-drop), non-video 3/6 match (sisanya post off-topik → unverified → dibuang THOTH), validate PASS. Tanpa perubahan Rust. Docs: scout `TOOLS.md`, memory.*
*Last updated: 2026-06-07 (gg) — PIPELINE SCOUT PENUH JADI TESTED SCRIPT (topik→video jadi). Glue terakhir `urls_to_contentset.js <topic_urls.json> [--main <url>]`: rakit content-set dari daftar URL → pilih 1 video sbg `main` (TikTok>YouTube; caption asli via oEmbed → isi title/description), sisanya `footage[]` dgn `is_video` (TikTok/YouTube=video cutaway; X/IG/FB=non-video→image card) + `relevance:"unverified"` → `output/content_set_<slug>.json`. **Pipeline lengkap, SEMUA tested script, end-to-end live-verified ("BPJPH"):** `discover_topics.js` (topik trending) → pilih topik → `topic_to_urls.js` (URL lintas platform query sama) → `urls_to_contentset.js` (rakit content-set) → `enrich_image_paths.js` (crop non-video) → `validate_content_set.js` → `THOTH run --content`. Bukti: content-set main TikTok + 14 footage (4 video + 10 non-video), enrich 10/10 crop OK, validate PASS errors=0 warnings=0. Docs: scout `TOOLS.md`/`trend-scout SKILL.md`, memory. Tanpa perubahan Rust.*
*Last updated: 2026-06-07 (ff) — DISCOVERY TOPIK→URL JADI SCRIPT TERUJI (scout), bukan cuma prosedur skill. `discover_topics.js [--top N] [--platforms x,youtube]` → topik trending dari browser: X `/explore/tabs/trending` (`[data-testid="trend"]`, topik bersih) + YouTube home popular (⚠️ YT hapus `/feed/trending`→redirect home→proxy). `topic_to_urls.js "<q>" [--platforms tiktok,tw,ig,fb] [--max N]` → 1 topik, query SAMA lintas platform, gabung URL post nyata → `output/topic_urls_<slug>.json` (`{platforms,all:[{platform,url}]}`); orkestrasi `search_tiktok_v2`/`search_social_v2` via child_process + FB inline (`/search/posts?q=`); platform tab tak attach di-skip tanpa abort. Live-verified "BPJPH": tiktok 5+tw 5+ig 3+fb 2 = 15 URL. Chain end-to-end scout: discover_topics → pilih topik → topic_to_urls → rakit footage[] → enrich_image_paths → validate_content_set → `THOTH run`. Docs: scout `TOOLS.md` + memory. Tanpa perubahan Rust.*
*Last updated: 2026-06-07 (ee) — WIRING crop post non-video ke content-set (scout `enrich_image_paths.js`). `crop_post.js` di-refactor jadi MODUL (`cropPost({url,out})`) + CLI. `node enrich_image_paths.js <content_set.json> [--force]` mengisi `image_path` otomatis untuk SEMUA entry non-video (`main`+`footage[]` dgn `is_video:false` & URL X/IG/FB & belum ada crop) via DOM crop; entry video / sudah-ada-crop / platform-tak-didukung di-skip; gagal per-entry (tab belum attach) di-skip tanpa abort; idempotent. Urutan hand-off final: rakit content-set → enrich → lint → `THOTH run`. Live-verified: 3 entri non-video (X main + IG + FB footage) ke-crop pixel-perfect, 1 footage YouTube (is_video:true) di-skip, lint PASS, re-run idempotent. THOTH sudah render `image_path` non-video sbg kartu statis (entry `y`), jadi end-to-end nyambung. Docs: scout `TOOLS.md`/`content-sourcing SKILL.md` (hand-off +langkah enrich), memory. Tanpa perubahan Rust.*
*Last updated: 2026-06-07 (dd) — CROP POSTINGAN NON-VIDEO via DOM (scout `crop_post.js`), gantikan vision_crop. Konteks: `image_path` untuk postingan non-video (tweet/foto IG/status FB) dulu diproduksi `vision_crop.js` (qwen3-vl screenshot→box). Test live (topik trending "BPJPH", query sama lintas platform) membuktikan vision TIDAK ANDAL: qwen3-vl 8b DAN 235b, prompt asli & diperketat, semua nge-box menu nav, bukan postingan. Solusi: `crop_post.js` baru — crop kartu post pixel-perfect dari DOM via CDP (teknik sama dgn scraper komentar: page-coords + captureBeyondViewport + blank-guard + retry). Live-verified pixel-perfect: X (kartu tweet penuh), IG (kartu post penuh — IG hapus `<article>`, anchor = panjat dari media terbesar ke ancestor ber-`a[/user/]`+`<time>`), FB (post penuh — anchor = panjat dari blok media `role=article` ke ancestor ber-profil-link+teks; ⚠️ FB kadang nyuntik survei relevansi + sliver komentar). `cdp.js connect({match})` dapat array + `requireMatch`. `vision_crop.js` dipertahankan HANYA sbg fallback (model override `THOTH_VISION_MODEL`). Metodologi: tentukan 1 topik trending di awal (dari trending X) → query SAMA di semua platform → crop URL non-video. Tidak ada perubahan Rust. Docs: scout `TOOLS.md`/`content-sourcing SKILL.md`, THOTH `CLAUDE.md`, memory.*
*Last updated: 2026-06-07 (cc) — SCRAPER KOMENTAR MULTI-PLATFORM (scout, upstream THOTH). Sebelumnya hanya TikTok (`scrape_comments.js`). Kini ada engine bersama `comment_engine.js` + 5 adapter tipis: `scrape_comments_x.js` (X/Twitter), `_yt.js` (YouTube watch+Shorts), `_ig.js` (Instagram), `_fb.js` (Facebook), `_reddit.js` (Reddit). Semua hasilkan content-set objek-tunggal ke `output/` + crop pixel-perfect ke `output/crops/`, format & perilaku identik TikTok (author/text/likes/avatar + `image_path`, sticker-skip) → langsung dikonsumsi fitur paste-crop THOTH (entry `aa`). `cdp.js connect({match})` di-upgrade: terima ARRAY domain + `requireMatch:true` (tak salah-drive tab lain). Gotchas krusial yang ditemukan saat debugging live: (1) crop BLACK di layout lebar/virtualised (YouTube watch) dgn viewport-coords+fromSurface → FIX pakai PAGE coords (`rect+scrollX/Y`) + `captureBeyondViewport:true`; (2) X & IG virtualise (recycle node off-screen) → tag-semua-lalu-loop gagal → engine kini CAPTURE PROGRESIF (extract mounted → crop segera → scroll → ulang, dedupe by key); (3) blank-guard: PNG <2KB → retry 1x lalu `image_path` kosong (fallback kartu drawtext); (4) YouTube Shorts: halaman tak scroll, harus klik tombol komentar buka panel. Live-verified: YouTube (Shorts 5/5 + watch 4/4 crop), X (4/5 crop, 1 blank→fallback, data 5/5), Instagram (5/5), Facebook (2/2, yield tergantung post). Reddit BELUM live-test (tak ada tab attached). Tidak ada perubahan kode Rust. Docs: scout `TOOLS.md`/`content-sourcing SKILL.md` + memory.*
*Last updated: 2026-06-06 (bb) — HOUSEKEEPING OUTPUT SCOUT (anti-sampah). Skrip workspace scout (`scout/`) dulu membuang content-set JSON + crop ke ROOT workspace (dan `batch_pipeline.js` malah ke ROOT repo THOTH) + folder `overlay_comments/` → berserakan. Kini SEMUA artefak terpusat di `output/` (JSON) + `output/crops/` (PNG), mengikuti konvensi yang sudah dipakai skill google-trends. Impl scout: (1) modul baru `paths.js` — `outPath(name)`→`output/`, `cropPath(name)`→`output/crops/` (auto-mkdir; path absolut dihormati). (2) di-wire ke 7 skrip: `scrape_comments.js` (crop→`output/crops/`, default JSON `output/THOTH_content_set.json`), `crop_comment_pipeline.js`, `batch_pipeline.js` (per-topik JSON pindah dari repo THOTH → `output/`), `search_tiktok_v2.js`→`output/tiktok_urls.json`, `search_social_v2.js`→`output/{ig,tw}_urls.json`, `find_viral_urls.js`→`output/viral_urls.json`, `vision_crop.js` (default crop→`output/crops/`). Migrasi: artefak test lama (5 crop + content-set) dipindah ke `output/` (image_path di-repoint), scratch `ss_*.png` + `overlay_comments/` di-recycle → root workspace bersih (cuma tersisa `output/`). 8 file `node --check` OK; linter `output/THOTH_content_set.json` PASS (semua image_path resolve). Doc: THOTH `CLAUDE.md` (sub "Lokasi file hand-off") + scout `TOOLS.md`/`content-sourcing SKILL.md` diupdate. Tidak ada perubahan kode Rust (THOTH terima path absolut apa pun via `--content`).*
*Last updated: 2026-06-06 (aa) — PASTE CROP ASLI KOMENTAR (real comment screenshot). Sebelumnya komentar di-render ULANG dari teks (drawtext di `comment_card.rs`); kini bila scout menyediakan crop screenshot komentar nyata, THOTH menempelkan crop itu sebagai kartu (anti-kaku, persis tampilan TikTok). Data author/text/likes TETAP dipakai (narasi grounding + ranking) — crop hanya mengganti VISUAL kartu. Impl: (1) `content_search.rs` `CommentInfo` +field `image_path` (`#[serde(default)]`); (2) `comment_card.rs` `CommentData`+`CommentCard` +`image_path`, +`CommentCard::has_crop()` (path non-kosong & file ada) + `card_rect(w,h)` (geometri zona kartu top-centre), `build_comment_filter` early-return "" bila `has_crop()` (lewati panel/teks/avatar sintetis); (3) `main.rs` passthrough `image_path` ke `CommentData` (TANPA download — crop sudah lokal); (4) `service.rs` 2 site (per-clip + narrator-driven) set `card.image_path` + SKIP avatar badge bila `has_crop()` (avatar sudah ada di crop); (5) `ffmpeg.rs` +`build_comment_image_overlay` (`-loop 1` input, `scale={card_w 0.84*W}:-2`, overlay `x=(W-w)/2:y={0.10*H}` `eof_action=pass` + enable-window), gate filtergraph +`comment_cards.iter().any(has_crop)`, chain SETELAH avatar badges (`cm_base_idx = badge_base_idx + badges.len()`), input crop di-append ABSOLUTELY LAST. Graceful: file hilang → fallback kartu drawtext (test `no_crop_when_path_missing`). Sisi scout: `scrape_comments.js` (DOM/CDP, bukan vision) hasilkan author/text PERSIS + likes + avatar_url asli + crop pixel-perfect per komentar (CDP screenshot `clip`) → isi `image_path`; output content-set OBJEK TUNGGAL (bukan array — `load_content_set` parse satu objek). `cargo check` OK; 90 unit test lulus (+2 comment_card: `crop_skips_synthetic_card`, `no_crop_when_path_missing`); build_cuda.bat OK (51.56s, zero error). Catatan: belum diverifikasi via render penuh (butuh main downloadable + Whisper/LLM) — wiring identik dgn image_cards/image_badges yg sudah proven.*
*Last updated: 2026-06-05 (z5) — VERIFIKASI KONFORMANSI NARASI (feedback loop): cek apakah narasi hasil THOTH sudah sesuai struktur korpus `narration_structures`. (1) Rust: `generate_narration` (`pipeline/mod.rs`) kini persist naskah penuh ke `narration/narration.txt` (selain hook.txt + words.json) supaya bisa diverifikasi. (2) `scripts/verify_narration_structure.py` (BARU): ambil narasi hasil (dari `--text` / `--narration-file` / `--job-dir` / `--latest` → baca narration.txt, fallback rekonstruksi dari narration_words.json), bedah struktur via LLM (skema beat-arc sama, reuse helper `analyze_narration_structure.py`), tarik NORMA korpus (avg quality, range beat_count, distribusi hook_format/style) + 3 struktur referensi terdekat (embedding cosine) beserta lessons-nya, lalu skor 9 cek konformansi: hook format kuat, NO moral preaching (anti-pattern #1), closing bukan nasihat/alay, memancing debat, punchline tak lemah, kedalaman arc (HOOK+penutup), tanpa kata formal, pacing 2.5-3.5 wps, style ada di korpus → verdict PASS/WARN/FAIL + conformance/10 + fix konkret per pelanggaran + arc referensi target. Report-only (tak nulis DB); `--json` utk dump. Diverifikasi: contoh rage-bait bagus → PASS 10/10; contoh ceramah+sapaan alay+kata formal → FAIL 6.7/10 (ketahuan hook='other', moral preaching, 4 kata formal, debat lemah). Korpus sudah tumbuh ~170+ struktur. 88 test lulus, build_cuda OK (49.7s).*
*Last updated: 2026-06-05 (z4) — NARRATION STRUCTURE RAG: narasi kini di-ground ke korpus struktur narasi terbukti. Pipeline data: `scripts/analyze_narration_structure.py` (kirim URL referensi → yt-dlp+ffmpeg → Groq Whisper verbose_json → LLM bedah BEAT-ARC [HOOK→SETUP→KONTEKS→INSIDEN→middle fleksibel→PUNCHLINE] + hook_format/narrator_posture/punchline/controversy/lessons + engagement faktual → Novita embed 4096-d → Supabase tabel BARU `narration_structures`, terpisah dari `viral_moments`; upsert by source_url; json_repair utk JSON bandel; escalating max_tokens 8k→24k utk reasoning model; quality 0-10). Sisi Rust (retrieval+inject): (1) `rag/store.rs` +struct `NarrationRef` + `retrieve_narration_structures()` (cosine halfvec di `narration_structures`, extract arc_template/hook_format/posture/quality + jsonb `what_makes_it_work`/`lessons_for_generation`) + `format_narration_refs()`. (2) `rag/mod.rs` export `NarrationRef`. (3) `narration/mod.rs` `produce()`+`generate_script()` +param `structure_refs` → blok "REFERENSI STRUKTUR NARASI YANG TERBUKTI (tiru pola, jgn salin kata)" disuntik ke prompt narator setelah grounding. (4) `pipeline/mod.rs` +`build_narration_structure_refs()` (embed source_text → retrieve top-N → format) dipanggil sblm `produce`. (5) `config.rs`/`config.toml` `[narration]` +`structure_rag`(default true)/`structure_rag_count`(4)/`structure_rag_min_similarity`(0.0). Gating: `structure_rag && THOTH_SUPABASE_URL ada && embed valid` — INDEPENDEN dari `[vector_db] enabled` (itu RAG momen); degrade diam bila tak tersedia, narasi tetap jalan. Diverifikasi LIVE: query "korupsi bansos KPK" → retrieve arc rage_bait kriminal (sim 0.567) + 3 struktur relevan, arc_template/quality/what_makes_it_work ter-extract benar. 88 test lulus, build_cuda OK (44.4s, zero warning). Korpus awal: 26 video Animelorian (commentary 14/rage_bait 7/storytelling 5, avg q≈8).*
*Last updated: 2026-06-05 (z3) — RC-2: COOKIES utk downloader footage (fix temuan RC-2 di `ANALISIS_PIPELINE_SCOUT_KORUPSI.md`). Masalah: footage cutaway di-download yt-dlp TANPA cookies → TikTok/IG (login-gated) tolak download → `fetch_overlay_from_url`→`None`→0 kartu montase, padahal video UTAMA berhasil (jalur ingest pakai `config.ingest.cookie_file`). Fix: jalur footage kini pakai cookies yang SAMA dgn ingest. (1) `overlay.rs` 3 downloader (`download_clip_section`, `download_clip_variant`, `download_clip_direct`) +param `cookie: Option<&CookieSource>`; builder chain dipecah utk sisipkan `with_cookie()` async + helper `apply_cookie()` + `cleanup_temp_cookie()` (hapus temp DB copy Windows-Chromium pasca-download, best-effort). (2) `fetch_overlay_from_url`+`fetch_overlay_clip` +param cookie, diteruskan ke downloader internal (TikTok search/direct/YouTube fallback semua dapat cookie). (3) `EditService` +helper `overlay_cookie()` (resolve `CookieSource` dari `config.ingest.cookie_file` > `cookie_browser`, mirror `ingest/service.rs`); di-resolve sekali di `run()` + `render_narration_video()`, diteruskan `.as_ref()` ke 4 call-site (2 per-clip montage + query fallback + narrator montage). Graceful: tanpa cookie config → `None` → perilaku lama; File source (kasus umum) tak bikin temp file. `crate::ingest::CookieSource` sudah re-exported. `cargo check` OK; full suite 88 test lulus; build_cuda.bat OK (41.0s, zero warning). Kombinasi z+z2+z3 = footage relevan (scout keyword) + bisa di-download (cookies) + narasi grounded (title/desc/komentar/visual) → dua keluhan utama (narasi ngawur + footage tak dipakai) tertangani.*
*Last updated: 2026-06-05 (z2) — Narasi grounding +DESKRIPSI VISUAL & ANALISA MOMEN (lanjutan z). Selain title/desc/komentar, narasi kini juga di-feed: (a) `[Deskripsi Visual]` = deskripsi per-frame dari vision model (`describe_video`) — apa yang TERLIHAT di layar, krusial utk b-roll tanpa suara; (b) `[Analisa Momen]` = ranked viral angle + `visual_score.note` tiap momen dari `moments.json`. Masalah lama: `vis_descriptions` (vision) cuma dipakai in-memory utk re-rank momen lalu DIBUANG — tak pernah sampai ke narasi. Fix: (1) `vision.rs` `VideoDescription` +derive `Serialize,Deserialize`. (2) `analyze/service.rs` persist `vis_descriptions` → `analyze/video_descriptions.json` setelah tulis moments (best-effort, additive). (3) `job.rs` +`video_descriptions_path()`. (4) `pipeline/mod.rs::generate_narration` baca `video_descriptions.json` (≤20 baris `[ts] desc`) + `moments.json` (top-4 momen, prefer vision note else `reason`) → blok `[Deskripsi Visual]` + `[Analisa Momen]` disisipkan antara komentar & transkrip. (5) Prompt `narration/mod.rs` daftar blok baru + grounding rule. Sumber ini lahir dari THOTH (stage analyze), jadi aktif juga utk run `--url` biasa (tak butuh scout). `cargo check` OK; full suite 88 test lulus; build_cuda.bat OK (42.6s, zero warning).*
*Last updated: 2026-06-05 (z) — NARASI GROUNDING dari title+description+komentar (fix RC-1 di `ANALISIS_PIPELINE_SCOUT_KORUPSI.md`). Masalah: raw b-roll tanpa voiceover → transkrip Whisper nyaris kosong ("Terima kasih.") → LLM narasi mengarang hook ngawur ("5 Pernyataan Paling Aneh Hari Ini", lepas topik BGN/MBG), sementara 17 komentar + title dibuang dari jalur narasi. Fix: `generate_narration()` (`pipeline/mod.rs`) kini menyusun `source_text` dari GABUNGAN blok berlabel — `[Judul]`(`main.title`) + `[Deskripsi]`(`main.description`) + `[Komentar Netizen Teratas]`(top-12 by likes dari `content_comments.json`, di-sort desc) + `[Transkrip Audio]`(hanya jika ≥8 kata) + `[Video Terkait]`(subtitle enrichment) — bukan transkrip saja. Empty-transcript tak lagi langsung `bail`: jalan selama ada title/desc/komentar. Prompt LLM (`narration/mod.rs`) diberi header multi-blok + instruksi WAJIB GROUNDING ("narasi berdasar fakta nyata di atas, DILARANG mengarang topik/tokoh di luar konteks"), char-cap dinaikkan 2400→3500. Impl: (1) `content_search.rs` +field `description: String`(`#[serde(default)]`) di `MainVideo` + `LoadedSet.{main_title,main_description}` + struct `MainContext{title,description}` + `MAIN_CONTEXT_FILE="content_context.json"` + `load_main_context()`. (2) `main.rs` tulis sidecar `content_context.json` dari title+description saat `--content`. (3) Narasi baca via `load_main_context` + `comment_card::load_comment_pool`. Graceful: run `--url` (tanpa content-set) → sidecar tak ada → fallback transkrip spt sebelumnya. Sisi scout (akuisisi): `MEMORY.md`/`content-sourcing/SKILL.md` — `main.description` WAJIB diisi (caption asli, bawa topik saat audio kosong), footage = hasil search dari keyword penting yg diekstrak dari title+description+komentar (isi field `query`), bukan video acak. THOTH `CLAUDE.md` diupdate (sub-section "Narasi grounding"). `cargo check` OK; content_search 6 test lulus (+`loads_main_title_and_description`); build_cuda.bat OK (1m09s, zero warning). Catatan: RC-2 (cookies footage downloader) BELUM dikerjakan — perbaikan terpisah.*
*Last updated: 2026-06-05 (y) — IMAGE CARD untuk postingan non-video (kontrak `image_path` scout, end-to-end). Kalau footage `is_video:false` (tweet teks/foto IG/artikel), yt-dlp tak bisa download → scout screenshot + vision-crop postingan, simpan PNG, kirim path di field baru `image_path`. THOTH kini render PNG itu sbg KARTU statis center (sejajar footage card, sumber still). Impl: (1) `content_search.rs` +field `image_path` (`#[serde(default)]`) di `ContentResult` & `MainVideo` + `LoadedSet.main_image_path`. (2) `enrichment.rs` +`is_image_card()` (non-video, image_path ada & file exists) + `load_image_pool()` (subset disjoint dari pool video, sumber file sama `content_enrichment.json`, rank match dulu). (3) `ffmpeg.rs` +struct `ImageCardCue{path,at_sec,duration_sec,scale_pct}` + `AudioOptions.image_cards` + `build_image_card_overlay()` (looped still `-loop 1`, `scale=cardw:-2`, overlay center `eof_action=pass`, enable-window — TANPA trim/setpts beda dari footage card video). Index input KONSISTEN: `ic_base_idx = fc_base_idx + fc_cues.len()`, badge digeser ke `ic_base_idx + ic_cues.len()`; input di-append urut source…narration, footage_cards, **image_cards(`-loop 1`)**, image_badges(last). Gate filtergraph +`!image_cards.is_empty()`. (4) `service.rs` load `image_pool` (independen `overlay.enabled`), montase narrator-driven kini INTERLEAVE video↔image card (alternate by beat parity bila dua pool ada; pakai apa yg ada bila satu) + jalur per-clip tambah image card di konten clip (rotasi by index, di luar gate overlay krn tak perlu download). (5) `main.rs` log jumlah footage non-video + warn bila `main.image_path` di-set (main tetap ingest dari url). Verifikasi LIVE (build_cuda 54.6s, NVENC): content-set TikTok main + 1 footage twitter `is_video:false`+`image_path` (PNG sintetis tweet) → log "image-card pool: 1 cropped post(s) [twitter]", "Montage: 0 footage + 2 image card(s)" → frame t=9 = kartu tweet ("@LambeSahamjja / Kepala BGN ditangkap KPK / 27.8K Retweets 124K Likes") ter-composite center di kanvas + comment card atas; t=16 = window lewat → B-roll TikTok asli balik (enable-window benar). 87 unit test lulus (+3: content_search `loads_image_path_for_non_video`, enrichment `image_card_requires_existing_file`, ffmpeg `image_card_centred_looped_still`). Dokumentasi kontrak: THOTH `CLAUDE.md` + scout `MEMORY.md`/`content-sourcing/SKILL.md` (prosedur screenshot→vision-crop + prompt vision detail). Catatan: main non-video belum jadi sumber clip (perlu url downloadable utk transcribe).*
*Last updated: 2026-06-05 (x) — Cutaway pool platform-agnostic (fix temuan Item-1 di entry w). `enrichment::is_downloadable_video` sebelumnya allowlist `youtube|instagram` saja → footage TikTok/Twitter-video/dll di-drop → pool kosong → densitas montase tak aktif. Diubah ke model DENYLIST: kualifikasi bila `is_video && url≠kosong && platform ∉ {news,web,article,blog}` (page-scrape) → SEMUA platform video yt-dlp-able (youtube, instagram, tiktok, twitter/x, facebook, vimeo, twitch, dailymotion, …) kini jadi kandidat cutaway, bukan TikTok saja. Aman & graceful: `fetch_overlay_from_url` tetap yang download nyata via yt-dlp; bila platform/URL gagal di-fetch → return `None` → footage itu di-skip tanpa merusak render (match terlalu luas tak pernah fatal). Text tweet tetap ter-filter krn `is_video=false`. Test `filters_and_ranks` direvisi (twitter/tiktok video kini KEPT, news video di-drop) + test baru `page_scrapes_excluded` (news/web ditolak; youtube/instagram/tiktok/twitter/facebook/vimeo lolos). enrichment 3 test lulus, build_cuda OK (43.7s).*
*Last updated: 2026-06-05 (w) — Wire fitur (v) ke jalur NARRATOR-DRIVEN + verifikasi run nyata. Temuan: `EditService::run` return awal ke `render_narration_video()` saat `[narration] enabled`, sehingga loop per-clip (tempat profile/comment card di-pasang) tak jalan → kartu tak muncul di mode narasi (workflow aktif user). Fix: (1) `AudioOptions.comment_card: Option` → **`comment_cards: Vec`** (beberapa kartu komentar time-windowed per video tanpa bentrok); `build_video_filter` concat semua fragmen `build_comment_filter`. (2) `render_narration_video` kini terima `profile_override` + `comment_pool` → render **1 profile card** (real-data override, setelah hook) + **top-N komentar (≤3, sort by likes desc)** tersebar di reaction beat masing-masing dgn window sendiri + SFX `notification.mp3` + avatar badge. Verifikasi LIVE `THOTH run --content THOTH_content_korupsi_bgn_mbg.json --provider novita --max-clips 1 --layout vertical --language id --model large-v3 --social @vnsclip` (TikTok main, build_cuda.bat 43s, NVENC): clip 16.2s — frame t=6 profile card "kompascom/@kompascom" (handle ASLI dari scout, tile "K" krn avatar_url kosong = fallback benar) + nama raksasa; t=9 comment card "@Owner pempek… / BUBARKAN MBG.. SDH TERBUKTI LADANG KORUPSI / 6.5K"; t=12 comment "@kompascom / makan masakan ibu… / 44" (rotasi by-likes benar). Catatan: Item-1 densitas montase TIDAK aktif di run ini krn footage TikTok di-filter `enrichment::is_downloadable_video` (allowlist cutaway = youtube/instagram saja) → pool kosong; densitas baru terlihat bila footage youtube/instagram. `cargo check` OK, ffmpeg 9 + comment_card 6 test lulus, build_cuda OK.*
*Last updated: 2026-06-04 (v) — Animelorian iterasi REAL-DATA (PLAN_ANIMELORIAN_RESTRUCTURE 3 item tersisa) + lensa scout. Prinsip: scout = akuisisi DATA faktual eksternal, THOTH = render. (1) **Profile card real-data** — content-set `main.profile {name,handle,followers,avatar_url}`; `main.rs` download avatar → sidecar `content_profile.json`; `profile_card.rs` +field `avatar_path` + `load_profile_override()` + `avatar_rect()` (geometri tile dibagi via `card_geom()`), skip inisial bila ada foto; `service.rs` override `character_*` LLM dgn data nyata (anti-halusinasi follower) + push avatar sbg `ImageBadgeCue`. (2) **CommentCard** — modul BARU `edit/comment_card.rs` (panel screenshot putih + avatar + author + word-wrap text + like count format K/M) di zona atas-tengah (bebas di clip konten); content-set `comments[] {author,text,likes,avatar_url}` → sidecar `content_comments.json`; `service.rs` rotasi komentar ke tiap clip konten di beat reaksi + SFX `notification.mp3` + avatar badge. (3) **Densitas montase** — config `[animelorian] montage_max_cuts` (default 2); `service.rs` selain kartu primer [seg,2seg] tile hingga `max_cuts−1` footage POOL tambahan ke `footage_cards` di window berjarak → footage ganti lebih sering. Avatar foto asli di-composite via mekanisme baru `ImageBadgeCue` (FFmpeg `-loop 1` input + overlay di tail [outv], setelah meme; index `badge_base_idx = fc_base_idx + fc_cues.len()`, urutan input konsisten) — `build_image_badge_overlay()`. Semua additive + graceful (tanpa sidecar/avatar → fallback inisial/no-card; default identik utk run `--url` lama). Sisi scout: `content-sourcing/SKILL.md` keluarkan `profile{}`+`comments[]` (faktual, no fabrikasi). `cargo check` OK; 83 unit test lulus (comment_card 6, profile_card 5, content_search 4, ffmpeg 9). ⚠️ build_cuda.bat final dijalankan user. Catatan: card real-data aktif di jalur PER-CLIP (sama spt profile card lama); jalur narrator-driven belum di-wire comment card.*
*Last updated: 2026-06-04 (u) — Content discovery DIPINDAH ke scout (agent "scout"); search internal THOTH DIBUANG. `THOTH run` kini terima: (1) `--url <URL>` single-video (default, tak berubah) ATAU (2) `--content <set.json>` = content-set dari scout `{ "main": {"url":...}, "footage": [ContentResult...] }`. THOTH ambil `main.url` + tulis `footage[]` → `content_enrichment.json` (lokasi base dir == output_dir, sama spt dulu) → edit/narasi konsumsi POOL TANPA PERUBAHAN. Tanpa url/content → error (TIDAK ada lagi auto Twitter/X trending). DIHAPUS: `main.rs::resolve_query_to_url`, arg `--query`, jalur no-arg trending, file `ingest/{query_expand,twitter_trending,youtube_search}.rs`, fungsi search di `content_search.rs` (`search_content`/`split_main_and_enrichment`/`pick_main_video`/driver `social_search.py`). DIPERTAHANKAN: struct `ContentResult` (model pool) + loader baru `load_content_set()` di `content_search.rs`; `edit/enrichment.rs::load_pool` & `pipeline::fetch_enrichment_texts` (consumer, untouched); `TrendAnalyze`; config `[content_search]`/`[twitter]` (unused, serde-default → backward-compat). Sisi scout: skill `content-sourcing` keluarkan content-set ini lalu picu `THOTH run --content`. `cargo check` OK; content_search 3 test + enrichment 2 test lulus. ⚠️ build_cuda.bat final dijalankan user. Catatan: `scripts/social_search.py` kini orphan (boleh dihapus terpisah).*
*Last updated: 2026-06-01 (g) — Multi-platform content search (enrichment supplier untuk style Animelorian): `scripts/social_search.py` cari lintas YouTube/TikTok/Twitter/Instagram/News dalam satu envelope JSON. Engine `playwright` (default, cookies + webdriver masking) atau `scrapling` StealthyFetcher/Camoufox (undetected, adaptive selector) untuk platform sulit (TikTok/Instagram); `auto` pilih per-platform. Sisi Rust `src/ingest/content_search.rs`: parse envelope → `pick_main_video()` (YouTube>TikTok, filter durasi, max views) + `split_main_and_enrichment()` (sisanya jadi pool cutaway). Config `[content_search]` opt-in (default off), `[twitter]`. Diverifikasi live SEMUA 5 platform (combined run = 15 hasil, 3/platform): YouTube+News (Playwright), TikTok+Instagram (Scrapling StealthyFetcher, Fetched 200, tanpa warning), Twitter (Playwright). Twitter dibuat tahan-flaky: `search_twitter()` retry 2× + deteksi login-wall (`_twitter_login_wall`) + scroll lazy-load → konsisten 3/3/3 (sebelumnya kadang 0 krn `data/cookies.txt` X cuma punya guest token `gt`, TANPA `auth_token`/`ct0`; Instagram juga anonymous tanpa `sessionid` tapi Scrapling tetap dapat reel via /popular/<tag>). 77 unit test lulus, build_cuda OK (54.8s). Catatan: utk Twitter konsisten penuh perlu cookies login (auth_token+ct0); ekstraksi judul News masih ambil nama source. TODO depan: wire `content_enrichment.json` ke Stage-4 edit sbg cutaway.*
*Last updated: 2026-06-01 (h) — Relevance audit + filter. Uji `prabowo joget`: YouTube & News HIGHLY relevant (judul/snippet = footage Prabowo joget asli) — ini sumber MAIN video, solid. Twitter topical (teks cocok, video bisa tangensial). Instagram tag `/explore/tags/<tag>/` sering redirect ke `/popular/` → muncul konten Prabowo umum (bukan joget). TikTok = blind spot (cover img alt kosong di halaman search → tak terverifikasi). Fix: (1) ekstraksi caption Scrapling dari `img[alt]`/`aria-label` → IG kini punya caption; (2) `apply_relevance_filter()` di `social_search.py` buang hasil yg punya teks tapi NOL token query (mis. IG "Bicara diplomasi…" tanpa prabowo/joget di-drop), hasil ber-caption nol-match dibuang, hasil tanpa caption (TikTok) di-keep & ditandai `relevance=unverified`; flag `--no-relevance-filter` & `--drop-unverified`. Field `relevance` (match|unverified) ditambah ke tiap hasil (serde Rust abaikan field tak dikenal → backward-compat). Stopword ID+EN. Catatan jujur: TikTok masih unverified (perlu fetch caption per-video / oEmbed); IG/Twitter butuh login utuh utk presisi.*
*Last updated: 2026-06-01 (i) — TikTok caption verification via oEmbed. `fetch_tiktok_oembed()` + `enrich_tiktok_captions()` (parallel ThreadPool, no auth) isi caption/author/thumb utk hasil TikTok yg tanpa teks → kini bisa difilter relevansi. TEMUAN PENTING: TikTok `search/video?q=` logged-out (cookies cuma `ttwid`, tanpa sessionid) MENGABAIKAN query — kembalikan 5 video feed generik IDENTIK utk query apapun ("Muterin Indomaret", "Disney WallE", dst). Sebelumnya lolos diam-diam sbg hasil palsu; sekarang oEmbed+filter buang semuanya → 0 false positive (lebih baik 0 drpd 5 salah). `finalize_and_emit()` jadi satu jalur exit: oEmbed → relevance filter → emit. Net combined run `prabowo joget` = 10 hasil semua `relevance=match` (YT 4, News 4, IG 2; TikTok 0 benar; Twitter flaky guest). Field `relevance` ditambah ke Rust `ContentResult` (serde default) utk prioritas enrichment. 77 unit test, build_cuda OK (43.3s). Kesimpulan: TikTok hanya berguna dgn cookies login TikTok; YouTube/News tetap tulang punggung relevansi.*
*Last updated: 2026-06-02 (j) — TikTok login cookies + VERDICT: web search HARD-BLOCKED. User beri cookies login TikTok manual (sessionid/sid_tt/odin_tt/msToken dst) → di-merge ke `data/cookies.txt` (27 cookie, ganti yg stale; file gitignored). Diuji menyeluruh dgn cookies VALID (sessionid confirmed loaded): `/api/search/item/full/` SELALU balas (200, body len=0) — kosong — meski (a) browser nyata Playwright Chromium, (b) warm-up homepage `/foryou` dulu, (c) stealth webdriver-mask, (d) scroll, (e) jalur hashtag `/tag/<tag>` (→ `/api/challenge/item_list/` jg 200 len=0). 28 anchor `a[href*="/video/"]` yg muncul = rail rekomendasi GENERIK (query-independent: "Muterin Indomaret"/"Disney WallE" identik utk query apapun). Akar: TikTok butuh signature request (X-Bogus/X-Gnarly + msToken signing dari webmssdk JS) + device-fingerprint verify; deteksi otomatis di level signature/TLS, BUKAN auth → cookies login tdk menyelesaikan. Verifikasi caption oEmbed (entry i) bekerja sempurna sbg pengaman: semua hasil generik di-drop → TikTok = 0 false-positive (graceful). Cookies login tetap disimpan (berguna utk yt-dlp download video TikTok spesifik nanti). REKOMENDASI: TikTok search via free web scraping TIDAK VIABLE; andalkan YouTube/News/IG/Twitter; utk TikTok perlu API berbayar (mis. RapidAPI TikTok) atau drop dari default platforms. Tdk ada perubahan kode Rust.*
*Last updated: 2026-06-02 (k) — TikTok di-hide dari default platforms + WIRE enrichment→Stage-4 cutaway. (1) Default platforms kini `youtube,instagram,twitter,news` (config.rs `default_cs_platforms`, config.toml, social_search.py `--platforms` default); tiktok opt-in eksplisit. (2) Enrichment wiring (fitur Animelorian "multi clip relevan"): modul baru `src/edit/enrichment.rs` `load_pool(base_dir)` baca `content_enrichment.json`, filter video downloadable (youtube/instagram, is_video, drop relevance=unverified), rank relevance=match dulu. `overlay.rs` +`fetch_overlay_from_url()` download URL kurasi langsung (cache md5(url), reuse download_clip_direct/trim). `service.rs`: load pool 1× (saat overlay.enabled) sebelum render loop; blok overlay kini prioritas pool (rotasi `pool[i % len]` → tiap clip dapat cutaway relevan beda) lalu fallback ke `overlay_query` yt-dlp search bila pool kosong/download gagal. Gate diperluas: overlay jalan bila `overlay_query` ADA ATAU pool ADA. Graceful + backward-compat (tanpa file → perilaku lama). Field `relevance` ContentResult dipakai utk ranking. 79 unit test (+2), build_cuda OK (54.4s).*
*Last updated: 2026-06-02 (p) — TTS Narrator-Driven Stage 1 (narasi spine) SELESAI & terverifikasi. Diagnosis: TTS "belum bekerja" krn `[reaction] enabled=false` + arsitektur lama cuma avatar post-roll. ElevenLabs sempat diblokir free-tier (abuse/VPN) → user subscribe → jalan. Impl Stage 1: (1) `reaction/tts.rs` +`synthesize_timed`/`synthesize_elevenlabs_timed` (endpoint `/with-timestamps` → audio_base64 + per-char alignment → agregasi ke `Vec<WordTimestamp>`) + fallback Edge via `scripts/tts_narrate.py` (sentence-boundary → distribusi kata) + `b64_decode` mandiri. (2) Modul baru `src/narration/` — `produce()`: LLM script komentator gosip 1-arc (`{hook, narration}`) → TTS timed → `Narration{mp3,words,duration,hook,text}`. (3) `NarrationConfig` `[narration]` opt-in (target_secs/language/duck_event_vol; provider/voice dari `[reaction.tts]`). (4) Pipeline stage 4.5 `generate_narration()` (best-effort) → simpan `narration.mp3`+`narration_words.json`+`hook.txt` di `job.narration_dir()`. Diverifikasi run nyata "prabowo joget"/novita: script 113 kata + hook "Mau jadi MC tapi malah jadi DJ", voiceover ElevenLabs 53.4s 128kbps, 113 word-timing presisi (last word end=53359ms=durasi). 82 unit test, build OK. TERSISA Stage 2: edit konsumsi narasi (audio spine + event di-duck + subtitle dari narasi + video di-pace ke narasi) — plan di `calm-foraging-sun.md`.*
*Last updated: 2026-06-02 (q) — TTS Narrator-Driven Stage 2 (edit narration-driven) SELESAI + prompt narasi diperbaiki. Prompt `narration/mod.rs` dirombak gaya "anak Jaksel react" + slang wajib natural (anjir/bre/tuh/sih/literally/sumpah/ngakak/fix/ya kali/gas) + larangan kata formal (merupakan/tersebut/beliau/dll) + few-shot → narasi jauh lebih hidup: "Anjir bre, kalian udah nonton belum? Niatnya mau keren-keren ajah eh malah jadi meme... Sumpah ngakak... Fix malu banget, ya kali bisa nggak malu. Gas!"; voice ElevenLabs lebih ekspresif (stability 0.28, style 0.50, speaker_boost). Stage 2: `AudioOptions.narration: Option<NarrationVoice>` + `encode_clip_direct` — narration jadi `[voice]` utama, event `[0:a]` di-duck (volume=duck_event_vol) + masuk amix sbg `[evt]`; narration input ditambah LAST (idx=paper_idx+anim). `EditService::render_narration_video()` (jalur baru bila `[narration] enabled`): render 1 video = durasi narasi, B-roll event di kanvas kertas + 1 footage card enrichment di tengah, subtitle word-pop DARI `narration_words.json` (0-based, sinkron narator), hook headline dari hook.txt. Diverifikasi via resume: clip 27.864s (=durasi narasi, audio AAC=narasi bukan event 92s), frame=kertas+footage card+subtitle \"IKUTAN\" (kata narasi di t-tepat). Alur video kini DIATUR TTS. 82 unit test, build_cuda OK (44s). Iterasi depan: montase multi-footage per beat, hook tanpa subtitle 0-3s di narration path, ducking dinamis.*
*Last updated: 2026-06-02 (t) — Narasi rage-bait style Fuadbakh23. Analisa referensi BSKWH1dk-tI (49.5s): (1) Hook = ranking/list ("Top dua kata-kata Asbun...") bukan sapaan — langsung bikin penasaran; (2) Isi = SINIS+HERAN bukan empati, kutip verbatim pernyataan kontroversial + reaksi heran singkat ("Anjay janggal", "Bisa-bisanya"); (3) Penutup = PERTANYAAN MEMANCING DEBAT atau biarkan fakta bicara sendiri — BUKAN ceramah/nasihat/moral. "Fix, kita harus lebih selektif... Mantul!" adalah pola SALAH. Prompt dirombak: system = "kreator rage-bait sinis/skeptis/heran", user prompt = struktur 3 bagian (hook ranking/gila → isi sinis+kutip verbatim+reaksi pendek → penutup WAJIB pertanyaan atau fakta akhir tanpa nasihat) + larangan keras (kata kasar, bahasa formal, ceramah, nasihat, 'mantul/gaspol/ygy' alay) + referensi vibe konkret. Hasil: hook "Bisa-bisanya Mama Gufron ngaku bisa video call sama malaikat maut", penutup "Menurut kalian, ini normal ga sih?" — BERSIH dari nasihat/alay. 82 test, build OK.*
*Last updated: 2026-06-02 (r) — Narrator-driven iterasi #1+#2. (#1 Montase multi-footage): `FootageCardCue` + `AudioOptions.footage_cards` + `build_footage_card_overlay()` (kartu center full-width, enable-window, di-chain seperti meme chain, input setelah narration di `fc_base_idx`). `render_narration_video` kini jadwalkan ≤4 footage card enrichment bergantian (main beat ↔ card beat tiap `montage_segment_secs`) → footage GANTI di sepanjang narasi (diverifikasi frame: t=7 podium card, t=13 event desa — beda). Kartu center (band tengah) tak menutup subtitle (band bawah). (#2 Hook tanpa subtitle): di `render_narration_video` kata narasi dalam window hook di-drop dari ASS (`start_ms>=hook_dur`) → hook headline baca sendiri. (#3 ducking dinamis): DITUNDA — butuh deteksi peak audio event. 82 unit test, build OK.*
*Last updated: 2026-06-02 (s) — Narasi clean (Gen Z, no kata kasar) + #3 ducking dinamis (lead-in). Prompt `narration/mod.rs`: hapus umpatan (anjir/anjay), larangan keras kata kasar, slang Gen Z CLEAN (gais/literally/sih/tuh/no debat/vibes/mantul/ygy) → diverifikasi grep BERSIH: "Gais, abis dengerin ini deh kalian pasti ngakak... No debat, ini vibes-nya mantul banget." #3: temuan — narasi ElevenLabs MENERUS (0 gap >0.45s, max gap 0.33s di batas kalimat) → leak per-jeda tak efektif. Solusi LEAD-IN: `NarrationConfig.lead_in_secs`(1.6)+`leak_event_vol`(0.45); `NarrationVoice.{leak_windows,leak_vol,lead_in_secs}`; `encode_clip_direct` [voice] di-`adelay` lead-in + [evt] volume ekspresi `if(gt(between(t,...)+...,0),leak,duck):eval=frame`; `render_narration_video`: event main keras `lead`s di awal (leak_window [0,lead]) lalu narator masuk (adelay) & event duck, subtitle digeser `+lead` (generate_ass clip_start=-lead), hook headline span [0,lead+hook_win]. Diverifikasi volumedetect: lead-in [0-1.5s] mean -24.6dB (event saja), narasi [3-5s] -17.6dB (narator dominan). Video 40.6s=narr+lead. 3 iterasi narrator-driven (montase/hook-clean/ducking) komplet. 82 test, build_cuda OK.*
*Last updated: 2026-06-02 (m) — ANIMELORIAN COMPOSITE MODE (paper canvas + footage montase). Feedback user: base bukan footage full-frame tapi KANVAS kertas hitam kusut (`assets/ui/Crumpled-Black-Paper-Stop-Motion-Anim.mp4`), footage = KARTU tengah, style = reaction dgn kumpulan footage. Impl A+B: (A) `AnimelorianConfig` `[animelorian]` opt-in (paper_bg/footage_scale_pct/hook_fullscreen/montage/montage_segment_secs/placement_variation); `build_video_filter` branch baru = footage `scale=cardw:-2[fg]` + paper `[paper_idx:v]...crop=1080:1920[bg]` + `overlay shortest=1` (WAJIB: paper `-stream_loop -1` infinite → tanpa shortest encode tak pernah selesai, ditemukan saat test clip invalid moov-atom); `AnimelorianRender` di AudioOptions; paper input ditambah LAST (paper_idx = hitung semua input sebelumnya). (B) montase: `OverlayStyle::FootageCard` (center, enable-window) promote enrichment dari Pip pojok → kartu penuh yg COVER main saat [seg,2·seg] (cut bersih, y_offset disamakan main). Hook (i==0) tetap full-frame immersive (jalur blur-self existing). Diverifikasi full-pipeline "prabowo joget"/novita: hook full-frame, konten = kertas+footage card+headline multi-warna+subtitle di margin (cocok referensi t=27s), montase cut ke footage enrichment relevan. 82 unit test (+3), build_cuda OK. Config default: animelorian+overlay+content_search enabled.*
*Last updated: 2026-06-02 (n) — Animelorian struktur naratif (Part A) + placement konsisten (Part C). Analisis ulang frame referensi (`test/analysis/ANIMELORIAN_STRUCTURE.md`): headline raksasa HANYA di hook, layer teks per-beat (tidak numpuk), placement HARUS konsisten (bukan random). Part A (`service.rs`): (1) `hook_title` di-gate hanya clip-0 (`is_hook`) saat animelorian — konten clip tak lagi dapat headline besar; (2) lower-third `headline` dimatikan total saat animelorian; (3) `callout`+`profile_card` di-skip pada hook (beat konten/intro); (4) subtitle berjalan DI-SUPPRESS selama window hook-title (`words.retain(start_ms>=cutoff)`) → hook = headline saja, subtitle muncul setelahnya (fix feedback "konten & headline jangan bersamaan"). Part C: hapus loncatan acak `[i%5]` y-offset → `card_y_off=0` TETAP (footage card di zona tengah konsisten, nyaman); FootageCard montase ikut y=0. Diverifikasi frame: hook t=1.5s headline+footage tanpa subtitle; hook t=8s subtitle muncul headline hilang; konten clip paper+card tengah tanpa headline besar + subtitle bawah. Plan lengkap di `PLAN_ANIMELORIAN_RESTRUCTURE.md` (Part B meme/humor + iterasi menyusul). 82 unit test, build_cuda OK (44s).*
*Last updated: 2026-06-02 (o) — Animelorian Part B: meme/humor by ekspresi (anti-kaku). Diagnosis: infrastruktur meme cue (katalog 12 meme + `asset_cues` schema + render MemeCue) SUDAH lengkap, tapi LLM cuma menaruh SFX (nol meme) krn panduan prompt konservatif ("calm clips can have none"). Fix: (1) `asset_catalog.rs::to_prompt_section` ditulis ulang — WAJIB ≥1 meme reaksi/clip di puncak emosi + cheat-sheet EMOSI→meme (kaget→screaming-woodchuck/Yea-Boi, kecewa→facepalm/crying/bruh, bingung→Nick-Young, bangga→clapping/Leo-pointing, panik→sweaty-gamer/keyboard-smash, realisasi→think-about-it, skip→no-signal) + aturan "land ON peak word, pair SFX, durasi 0.8-2s, match emosi jujur". (2) `service.rs` posisi meme KONSISTEN `top_right` (bukan rotasi corner acak — itu placement random yg dikeluhkan) + durasi clamp 0.8-2s (pop singkat). Hasil run: LLM naik dari 3→6 asset_cues, kini PLACE meme emotion-matched (Yea-Boi/agree_react @victory, think-about-it/realization). Diverifikasi frame: meme PiP kanan-atas di margin kertas, konsisten, tak tabrak card/subtitle. 82 unit test, build_cuda OK (41.6s).*
*Last updated: 2026-06-02 (l) — Full-pipeline test enrichment cutaway BERHASIL (diverifikasi frame). Query "prabowo joget" via novita: 20 hasil lintas-platform → 19 enrichment cand → main YouTube "Prabowo Berjoget Goyang Gemoy" (92s) → pool 5 video YouTube relevan → tiap clip dapat cutaway BERBEDA (rotasi index), render Pip kanan-bawah. Frame clip-0 t=11.5s: main(Prabowo joget) + headline + subtitle CapCut + Pip cutaway video lain relevan — semua komposit benar. BUG ditemukan & fix: `fetch_overlay_from_url` awalnya pakai `download_clip_direct` yg `--match-filter "duration<=N"` MENOLAK video enrichment full-length (yt-dlp exit 0 tanpa file → "not found on disk" → fallback). Fix: `YtDlpArgs::download_first_secs()` (`--download-sections *0-N` + `--downloader ffmpeg`) + fungsi baru `download_clip_section()` ambil hanya N detik awal (cepat, tak kena filter). Diverifikasi: download 6s clip = 6.3MB OK; re-run kedua clip dapat cutaway (4-Ows1roq-4, 2WFuqeX3N-I) tanpa error. Pipeline 2 clip selesai exit 0. build_cuda OK (47.7s).*
*Last updated: 2026-06-01 (f) — `THOTH run` URL optional: `--query` search YouTube via yt-dlp; tanpa URL/query → auto Twitter/X trending (ExplorePage API + cookies.txt Netscape) → search YouTube. `src/ingest/twitter_trending.rs` + `src/ingest/youtube_search.rs`. 72 unit test, build_cuda OK.*
*Last updated: 2026-06-01 (e) — Full-pipeline test (v3 Bigmo) berhasil: hook/profil/callout/SFX-cue semua ter-render benar (diverifikasi frame). Fix: (1) `main.rs` auto-set FONTCONFIG_FILE Windows agar drawtext `font=` jalan dgn ffmpeg lokal; (2) hook title aktif → headline panel dimatikan (anti-duplikat teks); (3) `at_sec` cue/callout: prompt diperjelas relatif-klip + konversi defensif jika absolut (>durasi). Catatan: presisi timing cue bergantung LLM (±2-3s); sumber sebaiknya footage mentah (bukan short yg sudah ber-overlay).*
*Last updated: 2026-06-01 (d) — Beat-3 Callout: `edit/callout.rs` render angka di kotak accent + glyph panah ◀▶▲▼ via drawtext, digerakkan schema `callouts[]` (text/at_sec/position/direction) dari LLM, opt-in `[callout]`. Divalidasi render+frame. Ketiga beat visual template (hook/profil/callout) kini lengkap. 67 unit test lulus, build_cuda OK.*
*Last updated: 2026-06-01 (c) — Beat-2 Profile Card: `edit/profile_card.rs` render kartu profil (avatar inisial + nama + @handle + stats) & nama raksasa di atas kepala via drawbox/drawtext, digerakkan schema `character_name/handle/stats` dari LLM, opt-in `[profile_card]`. Divalidasi render+frame (semua elemen tampil benar). Stats tanpa fabrikasi (hanya bila disebut). 63 unit test lulus, build_cuda OK.*
*Last updated: 2026-06-02 — Query keyword expansion: `src/ingest/query_expand.rs::expand_query()` menganalisis raw query via LLM → 5-8 keyword pencarian ganda sebelum multi-platform search (mis. "kata asbun ghufron al maqoli" → "ghufron viral","ghufron ceramah","MUI ghufron",..). Dipanggil di `resolve_query_to_url()` (main.rs) bila `[content_search] expand_keywords=true` (default), passing semua keyword ke `search_content()` → footage lebih luas + enrichment pool lebih kaya → konteks multi-sumber utk narasi rage-bait. Graceful fallback ke raw query bila LLM gagal. build_cuda OK (1m10s), 5 content_search test lulus.*
*Last updated: 2026-06-01 (b) — Beat-1 Hook Title: `edit/hook_title.rs` render judul raksasa multi-warna per-kata (Montserrat-ExtraBold, animasi pop), di-burn sbg subtitles pass kedua, opt-in `[hook_title]`. Divalidasi render+frame (font resolve OK, warna palette benar). 59 unit test lulus, build_cuda OK.*
*Last updated: 2026-06-01 — Asset terpusat di `assets/` (fonts/sfx/bgm/meme/ui). Anotasi aset `scripts/annotate_assets.py` (audio diukur dari envelope; meme video via Novita vision) → `assets/asset_catalog.json` (28 aset). Analyze stage inject katalog ke prompt (`analyze/asset_catalog.rs`) + field `asset_cues[]` ber-timestamp diisi & divalidasi LLM. Edit stage me-render cue AUDIO (`build_cue_audio_filter`, multi-SFX) **dan VIDEO** (`build_meme_overlay_filter`, meme PiP ber-timestamp di pojok bergilir, `MemeCue`). Keduanya divalidasi via ffmpeg run nyata + verifikasi frame visual (PiP tampil benar di pojok). Detail final: audio meme bersuara di-mix + narasi di-duck 50% di window meme; cue at_sec di-beat-snap saat beat_sync. 55 unit test lulus, build_cuda OK. Lihat PLAN_VIRAL_TEMPLATE_ENGINE.md.*
*Last updated: 2026-05-31 — News Enrichment Stage 4 Phase 1+2 selesai: keyword extraction + search Playwright + headless screenshot (conda env THOTH-news) + FFmpeg 9:16 formatter. 11 unit test lulus. Lihat PLAN_REACT_NEWS_PIPELINE.md.*
*Last updated: 2026-05-27 (4)*
*Status diperbarui: Style Profiles, CapCut subtitle, Reference Video Analyzer, Beat-sync, RAG — semua selesai.*
*Diperbaiki (2026-05-25):*
  - *vocab.rs: tambah "tepuk tangan" standalone ke intro keywords → talk show applause ceremony kini terdeteksi*
  - *service.rs: `snap_end_to_sentence_boundary()` di-upgrade pakai inter-segment silence gaps (≥300ms) sebagai primary boundary signal; connector-word scan jadi fallback*
  - *service.rs: `has_ceremony_transcript()` + filter baru — drop mid-video ceremony segments (live event roll-call, tepuk tangan buat [name], audience greeting) yang lolos dari timestamp-based intro filter*
  - *prompt.rs: tambah LIVE RECORDING ceremony warning; tambah INTERVIEW PANEL RULE — pilih JAWABAN bukan PERTANYAAN*
*Diperbaiki (2026-05-26) — Long-Video Temporal Coverage Fix:*
  - *service.rs: `select_temporally_diverse()` — gantikan pure quality-sort+truncate dengan 2-phase bucket selection: Phase 1 pilih satu clip terbaik dari setiap N time bucket (N=max_clips), Phase 2 isi slot kosong dengan kandidat terbaik dari bucket manapun → **coverage naik dari 9% → ~60-70% video, dead zone 24-menit di akhir video tereliminasi***
  - *service.rs: adaptive `chunk_size_secs` — video >45 min pakai 6-menit chunks (12 chunks) vs sebelumnya 3-menit (29 chunks); konteks LLM per chunk lebih besar, API calls lebih sedikit*
  - *service.rs: adaptive `clips_per_chunk` — >10 chunks = 2 clips/chunk; 6-10 chunks = 3; <6 = max_clips/2. Mencegah flooding kandidat dari early chunks*
  - *service.rs: `clips_overlap_significant()` — gantikan dedup `|start_diff|<10 AND |end_diff|<10` dengan overlap ratio >35%; catches kasus clip_000(357-398s) + clip_001(378-414s) yang share 20s konten tapi lolos check lama*
  - *service.rs: `snap_start_past_fillers()` — advance start_sec melewati run "Ya. Ya. Ya. Oke." di awal clip; waktu clip dishift forward, end_sec dikompensasi*
  - *vocab.rs: tambah "kami/kita membuat episode", "saat podcast ini", dll ke intro keywords → clip_007-like content (episode setup at t=78s) kini terfilter*
*Diperbaiki (2026-05-27) — FFmpeg Drawtext Headline % Rendering Fix (final):*
  - *ffmpeg.rs: `build_headline_filter()` — tambah `:expansion=none` ke setiap `drawtext=` call (social, hl1, hl2, source credit). Root cause: FFmpeg drawtext 2-phase parsing — Phase 1 option parser, Phase 2 text expander. `%` dalam teks (mis. "90% KASUS KORUPSI") masuk Phase 2 sebagai format specifier `%{...}`. Behavior versi-dependent: sebagian FFmpeg versi treat `%%` → `%` (benar), sebagian lain treat `%` + karakter non-`{` → invalid specifier → **seluruh drawtext element di-skip secara silent** → line 1 invisible sementara line 2 (tanpa `%`) tampil normal. Fix sebelumnya (`%%`) tidak cukup karena version-dependent. Fix final: `expansion=none` menonaktifkan Phase 2 sepenuhnya — `%` selalu literal, zero version sensitivity. Sekaligus: hapus `'%' => "%%"` dari `esc()` karena tidak diperlukan lagi.*
*Diperbaiki (2026-05-27) — Headline Quality & Truncation Fix:*
  - *ffmpeg.rs: `wrap_headline()` — `max_chars` 22 → 24 untuk vertical video. Root cause: max_chars=22 menyebabkan last-word drop pada headline valid ≤44 chars (contoh: "INVESTOR TOLAK INVESTASI KARENA RISIKO" 38 chars → line2 "INVESTASI KARENA"=16, +"RISIKO"=23 > 22 → "RISIKO" hilang). Dengan 24: "KARENA RISIKO"=13 ≤ 24 ✓*
  - *service.rs: tambah `sanitize_headline()` dipanggil dari `validate_and_clamp()` — enforce 44-char hard limit dengan word-boundary truncation + warn log; strip surrounding quotes dari LLM output*
*Last updated: 2026-07-07 (hhh) — FIX: render dekoratif hang 15–25 mnt (ROOT CAUSE sebenarnya). BUKAN deadlock multithread FFmpeg maupun mismatch framerate — DUA hipotesis itu TERBUKTI SALAH (run nyata tetap hang dengan `-filter_complex_threads 1`). Akar: satu input gambar `-loop 1` KORUP (avatar hasil download sampah, mis. `output/comment_avatar_2.png` = 13054 byte spasi ASCII → `ffmpeg -i` "Invalid PNG signature 0x2020202020202020") tak pernah bisa di-decode → tak pernah mengeluarkan frame → filter `overlay` yang mengonsumsinya memblokir SELAMANYA, independen jumlah thread. Intermiten (content-dependent: hanya komentar tanpa crop scout yang jatuh ke badge avatar legacy). Fix 2-lapis di `src/main.rs`: (1) `download_image_to` validasi MAGIC BYTES (PNG/JPEG/WEBP/GIF), tolak non-gambar (header Content-Type berbohong) → `None`; (2) download comment-avatar yang sudah usang DIHAPUS (kartu komentar sudah render dari crop scout `image_path`/`has_crop`). Konsekuensi (`src/edit/ffmpeg.rs`): karena threading tak pernah jadi penyebab, `-filter_complex_threads` DIAKTIFKAN LAGI = ncpu (`available_parallelism`, override `THOTH_FILTER_THREADS`) untuk kecepatan; normalisasi `fps=25,settb=AVTB` (byproduct hipotesis framerate yang salah) DIREVERT di keempat branch layout → overlay komposit di framerate NATIVE sumber (permintaan user). Watchdog `run_ffmpeg` (default 300s) tetap sebagai jaring pengaman untuk input never-EOF lain. Verified: `build_cuda.bat` EXIT=0 (0 err/0 warn), `cargo test --bin thoth` 97 passed.*

*Last updated: 2026-07-07 (ggg) — PERF_PLAN Fase D & E: async performance improvements:*
  - **Fase D — Edit: ffmpeg non-blocking** (`src/edit/service.rs`): tiga pemanggilan blocking ffmpeg di async context kini dibungkus `tokio::task::spawn_blocking`: (1) `generate_thumbnail` di loop per-clip (L1476), (2) `pick_cover_frame_time`+`generate_thumbnail` untuk cover subject (L1750), (3) `encode_clip_direct`+`generate_thumbnail` di `render_narration_video` (L2140). Mencegah pemblokiran Tokio worker thread saat render narasi.
  - **Fase E — join_all enrichment subs** (`src/pipeline/mod.rs`): `fetch_enrichment_texts` loop serial yt-dlp → `stream::iter.buffer_unordered(4)`; tiap kandidat mendapat sub-dir sendiri (`.narr_subs/<url_hash>`) untuk eliminasi tabrakan file antar future paralel.
  - **Fase E — Groq chunk concurrency** (`src/transcribe/service.rs`): `groq_transcribe_chunked` 2-fase: (1) extract semua chunk serial (FFmpeg stream-copy cepat), (2) upload ke Groq `buffer_unordered(3)` → sort by idx → dedup overlap → merge. Timestamp tetap kontinu; urutan terjaga via sort setelah collect.
  - *Build verified: `cargo check` EXIT 0 (0 err/0 warn); `build_cuda.bat` EXIT 0 (0 err/0 warn); `cargo test --bin thoth` 96 passed.*

*Last updated: 2026-06-27 (ddd) — Narration Cultural-Context Enrichment + Cultural Pulse + model audit:*
  - *scout `enrich_context.js`: decode subteks komentar (references entitas/meme/slang + per-comment context + discourse audience_stance) → blok narasi `[Konteks Budaya]`/`[Maksud Komentar]`; narator diinstruksi baca sarkasme & TIDAK menyalahkan netizen (fix kasus: narasi menyalahkan komentar "konoha"/"pak nadim"/"10+6").*
  - *`web_grounding.js`: refresh status entitas TERKINI via Google News (CDP) — atasi cutoff model (mis. Nadiem "menteri"→"terdakwa kasus Chromebook").*
  - *`ckb.js`: Cultural Knowledge Base di **Supabase Postgres** (tabel `ckb_entities`/`ckb_memes`/`ckb_pulse`, klien `pg`+SSL, auto-DDL) — cache-first lintas-run & lintas-mesin; fallback lokal-JSON. Setup workspace: `npm install pg` + `.supabase_url`/`THOTH_SUPABASE_URL`.*
  - *`pulse_harvest.js`: trend dari komentar feed (bukan view-index) → `ckb_pulse` + register gaya bahasa → `[Tren Diskursus]` (Priority 6 sisi diskursus).*
  - *Rust: struct `Reference`/`Discourse` (+`as_of_date`/`source_url`/`trends`) di `content_search.rs`, blok narasi di `pipeline/mod.rs`, prompt di `narration/mod.rs`; `LlmProvider::chat_completion_json` (response_format json_object) utk keandalan JSON analyze/narasi.*
  - *Model audit: analyze → `deepseek-v3.1`; vision frame-desc → `qwen2.5-vl-72b` (2026-07: DEPRECATED Novita → `qwen3-vl-235b-a22b-instruct`); task teks scout → `deepseek-v3.1`; cover default → `gemini-2.5-flash-image`.*
  - *Detail penuh: `RESEARCH_context_enrichment_narration.md`.*

*Last updated: 2026-07-08 (iii) — Narration prompt FLSR alignment (`src/narration/mod.rs`):*
  - RAG referensi struktur narasi ditandai inspirasi-saja; STRUKTUR WAJIB menang bila terjadi konflik dengan referensi.
  - ISI 3-tier di STRUKTUR WAJIB kini dibuka dengan tension-opener (kalimat gantung sebelum detail kasus mengalir).
  - PENUTUP dual-mode: pertanyaan tajam (kasus debat) ATAU value-close spesifik (kasus non-debat) — bukan nasihat generik.
  - REFERENSI VIBE diperbarui untuk mendemonstrasikan TENSION OPENER + struktur MODE A/B secara konkret.
  - Spesifikasi: `docs/superpowers/specs/2026-07-08-flsr-narration-prompt-design.md`.
  - *Verified: `cargo test --bin thoth narration` 4 passed/0 failed; `build_cuda.bat` EXIT 0 (0 error, `thoth.exe` rebuilt).*

*Last updated: 2026-07-11 (jjj) — FLSR implementation status audit (dari `FLSR_extracted.txt`, verifikasi via git log + baca source langsung, bukan asumsi):*
  - **Recreating / Anatomi Winning Content**: SUDAH diimplementasi, lihat entry (iii) di atas — TENSION OPENER, ISI 3-tier, PENUTUP dual-mode di `src/narration/mod.rs`. Scope-nya cuma prompt narasi.
  - **Finding + Listing**: TIDAK pernah diimplementasi atas nama FLSR, TAPI sudah ada fondasi fungsional yang overlap kuat — `thoth trend-analyze` command (`src/analyze/trend_analyzer.rs`, subcommand terdaftar di `src/cli.rs`, dispatch di `src/main.rs:720-723`, sejak commit `db8c956` 2026-05-23) + Style Profiles (`StyleProfile`/`StylesConfig` di `src/config.rs`, dikonsumsi nyata di `src/pipeline/mod.rs` dan `src/edit/service.rs`, bukan dead code). Fitur ini menganalisis video referensi → output profil TOML `[styles.profiles.<name>]` — secara fungsi ini "bedah konten top-tier jadi profil terstruktur", tapi **tidak pernah disambungkan ke narasi** (`style_profile` nol match di `src/narration/mod.rs`).
  - **KOREKSI roadmap di CLAUDE.md**: bagian "Prioritas yang BELUM diimplementasi" masih menandai 🔴 "Style Profiles" dan 🔴 "Reference Video Analyzer — thoth trend-analyze command" sebagai belum ada — ini **stale/salah**, kedua fitur itu sudah eksis dan wired sejak 2026-05-23, lebih dari sebulan sebelum edit terakhir CLAUDE.md (2026-07-09). Perlu diperbaiki saat CLAUDE.md berikutnya disentuh.
  - **Sorting (untuk referensi eksternal)**: belum ada — tidak ada mekanisme ranking/prioritas kandidat top-tier eksternal sebelum eksekusi (beda dengan ranking momen internal footage di `analyze/service.rs` yang sudah ada tapi untuk footage sendiri, bukan konten kompetitor/referensi).
  - **scout/ (Finding/Listing/Sorting tahap discovery)**: nol penamaan/konsep FLSR — belum disentuh sama sekali.
  - Follow-up belum dikerjakan (sudah tercatat di spec 2026-07-08 sebagai out-of-scope): `closing_type` (debate/value) tagging di `scripts/narration/analyze_narration_structure.py` untuk RAG corpus.
  - **Kesimpulan**: FLSR framework diimplementasi PARSIAL — hanya layer Recreating/narasi. Finding/Listing/Sorting end-to-end (menyambungkan trend-analyze + Style Profiles ke alur eksplisit, plus scout alignment) BELUM dikerjakan — perlu brainstorming/spec terpisah kalau mau dilanjutkan.

*Last updated: 2026-07-11 (jjj) — Dashboard Fase 1 (Control + Monitor) IMPLEMENTED. Spec: `docs/superpowers/specs/2026-07-11-dashboard-architecture-design.md`:*
  - **Workspace split** ✅ — crate tunggal `thoth` (bin) dipecah jadi Cargo workspace: `crates/thoth-types` (leaf: wire types `ProgressEvent`, pure-serde), `crates/thoth-core` (lib, `thoth_core`, semua tipe + orkestrasi + `run_cli()`), `crates/thoth` (bin CLI/worker tipis, `--features cuda`), `crates/thoth-server` (bin axum REST/SSE). thoth-server depend HANYA ke `thoth-types` (bukan thoth-core) → TIDAK link SATU pun heavy dep (ffmpeg/wgpu/CUDA/Whisper); worker jalankan pipeline out-of-process. Cancel = taskkill /T (bunuh seluruh process tree worker, bukan cuma thoth.exe).
  - **Worker channel `--progress-json`** ✅ — flag global; `ProgressEvent{stage,pct,message,ts}` NDJSON di stdout, log manusia di stderr. Emisi di choke point `stage_header()`. File: `crates/thoth-core/src/util/progress.rs`, `cli.rs`.
  - **thoth-server** ✅ — job executor spawn worker subprocess (`thoth <cmd> --progress-json`), parse NDJSON→broadcast SSE, redb `JobStore`, API-key auth (Bearer REST; query-token SSE), REST `/api/jobs*`+cancel, `/api/artifacts/:id/*path` (guard traversal via `Component::Normal`), serve SPA. File: `crates/thoth-server/src/{executor,store,job,auth,routes,lib}.rs`.
  - **dashboard/** ✅ — cockpit SPA Vite+React+TS+shadcn+Tailwind (Bun). RunForm/JobList/JobMonitor/LogPane; `api.ts` typed client (byte-match tipe Rust), SSE via `EventSource ?token=`. File: `dashboard/src/`.
  - **Kontrak interface** ✅ — ditambahkan ke CLAUDE.md ("Kontrak Interface: thoth-core ↔ adapters"): perubahan permukaan publik `thoth-core` WAJIB update kedua adapter Rust (dipaksa compiler) + `dashboard/src/api.ts` (manual, utoipa ditunda Fase 3).
  - **Ditunda by design**: Fase 2 (moments/rerender/player/charts) + Fase 3 (config/profiles/vocab/curators, utoipa/OpenAPI codegen) — di luar scope Fase 1.
  - *Verified: `build_cuda.bat` EXIT 0 (thoth.exe rebuilt + thoth-server.exe built, 0 error); `cargo test --workspace` 106 passed/0 failed; `dashboard` `tsc -b --force` 0 error + `bun run build` OK.*

*Update: 2026-07-12 — Worker/server REDESIGN: SQLite job-queue (dua proses peer independen). Spec: `docs/superpowers/plans/2026-07-11-worker-queue-sqlite.md`. Launch: `docs/RUNNING.md`.*
  - **`crates/thoth-jobs`** ✅ (baru) — antrian kerja di SQLite/WAL. Skema `jobs`+`job_events` (`migrations/`, `journal_mode=WAL`/`synchronous=NORMAL`/`busy_timeout=5s`), tipe `JobSpec`/`JobRecord`/`JobStatus`(+`Cancelled`)/`JobEvent`, `JobStore` (enqueue/`claim_next` atomic `UPDATE…WHERE id=(SELECT…LIMIT 1) RETURNING`/append_event/update_progress/heartbeat/finish/`reap_stale`/events_since). File: `crates/thoth-jobs/src/{lib,types}.rs`.
  - **Model dua-proses** ✅ — GANTI spawn-subprocess-per-job jadi `thoth-server` (REST/SSE) + `thoth worker` (engine hangat) yang **hanya** berbagi 1 file SQLite. Tanpa parent/child, tanpa stdio, tanpa supervisor. Warm models (no cold-start CUDA/Whisper tiap job). File: `crates/thoth-core/src/worker/mod.rs` (`run_worker` claim loop + `run_one` seam + `execute_pipeline`), `cli.rs` (`Worker(WorkerArgs)`).
  - **thoth-server → thoth-jobs** ✅ — buang `redb`/`executor.rs`/`store.rs`/`job.rs`; `routes.rs` create/list/get/cancel via `JobStore`, SSE `stream_job` tail `job_events` by `seq` + `Last-Event-ID`/`?after=` resume (~400ms poll, no LISTEN/NOTIFY). `reaper.rs` gagalkan job dgn heartbeat basi (net pengganti supervisor). sqlx `["runtime-tokio","sqlite"]` saja (zero heavy dep tetap terjaga).
  - **run_once + progress sink** ✅ — badan `Commands::Run` diekstrak ke `pub async fn run_once(args,config,cancel)` (dipakai identik oleh CLI & worker). `util::progress::set_sink()` — worker install DB-sink (tulis progress→`job_events`), CLI tetap stdout NDJSON.
  - **Cancel kooperatif + lintas-platform** ✅ — `taskkill /T` DIHAPUS. Cancel = flag DB `cancel_requested` yang di-poll worker (worker pemilik proses anak ffmpeg/whisper/python). Jalan identik di Linux/macOS/Windows, TANPA hack OS. Multi-worker aman (claim atomik).
  - **SPA** ✅ — `api.ts` `JobStatus` + `"cancelled"`; `StatusBadge` glyph ⊘ + warna `status-cancelled`.
  - *Verified: `build_cuda.bat` EXIT 0 (thoth.exe rebuilt + thoth-server.exe built); `cargo test --workspace` PASS; `dashboard` tsc 0 error + build OK; e2e smoke: server+worker share DB, POST /api/jobs → worker claim → SSE progress → terminal.*

*Update: 2026-07-13 — Dashboard Fase 2 (Review & QC, slice review-only) IMPLEMENTED. Spec: `docs/superpowers/specs/2026-07-12-dashboard-fase2-review-design.md`.*
  - **Injected job_id + flat layout** ✅ — `RunArgs` dapat `--job-id`; `PipelineRunner::run` param baru `job_id_override: Option<&str>`. Ada id → `JobContext::new_flat` (root = `output_dir` langsung, TANPA `.thoth/<id>`); tanpa id → tetap mint `Uuid` + `JobContext::new` (nested `.thoth/<uuid>`, default CLI tak berubah). `resume_id` & `job_id_override` ortogonal. Worker push `--job-id <job.id>` ke argv. File: `crates/thoth-core/src/{cli,lib,pipeline/mod,pipeline/job,worker/mod}.rs`.
  - **`GET /api/jobs/:id/manifest`** ✅ — handler tipis (tanpa dep thoth-core, filesystem-only) walk `output_root/<id>/`, balikkan relpath forward-slash utk artefak yang ADA: video (`clips/final_concat.mp4` → fallback `clips/clip_*.mp4` terbaru by mtime), thumbnail, moments (`analyze/moments.json`), narration (`narration/narration.mp3`), transcript (`transcribe/transcript.json`). Selalu 200; job absen/`:id` non-Normal (guard path-traversal) → `{}`. Auth Bearer sama spt route `/jobs*`. File: `crates/thoth-server/src/{routes,lib}.rs`.
  - **Review UI** ✅ — `dashboard/src/api.ts` tambah `type Manifest` + `getManifest()` (hand-synced dgn handler Rust). `ReviewPanel` (tampil hanya saat `status==="succeeded"`): player `<video>` via `fetchArtifact`→object-URL (bearer-aware, di-revoke on unmount/switch), tabel moments (#, title/headline, start–end, viral_type, energy, reason+hook), score-bar CSS `visual_score` (u8 0–10 → width `value*10%`, TANPA chart lib), tautan raw transcript/narration. Widget saling isolasi (video 404 / moments.json korup → notice lokal, sisanya tetap render). JobMonitor buang `ARTIFACTS` guess-list + `handleArtifact` lama. File: `dashboard/src/components/{ReviewPanel,JobMonitor}.tsx`.
  - **Ditunda by design**: edit narasi/subtitle in-browser + `POST /rerender` (partial re-run) — paruh berat Fase 2 asli; Fase 3 (config/profiles/vocab/curators, utoipa). Tanpa migration `thoth-jobs`.
  - *Verified: `build_cuda.bat` EXIT 0 (thoth.exe + thoth-server.exe rebuilt, mtime maju 07-12→07-13); `cargo test --workspace` 122 passed/0 failed; `dashboard` `bun run build` 0 error; live-binary smoke: `thoth-server.exe` + flat dir → `GET manifest` = `{"video":"clips/final_concat.mp4","moments":"analyze/moments.json"}`, unknown job → `{}`, tanpa auth → 401. Full pipeline-run→succeeded + review tab di browser = manual (butuh API keys/GPU), belum dijalankan.*

*Update: 2026-07-13 — Dashboard Render Parity + Config Editor (Operator Console sub-proyek A) IMPLEMENTED. Spec: `docs/superpowers/specs/2026-07-13-dashboard-render-parity-config-design.md`; Plan: `docs/superpowers/plans/2026-07-13-dashboard-render-parity-config.md` (7 task TDD, subagent-driven). Tujuan: dashboard bisa mengoperasikan ~12 knob `thoth run` + edit `config.toml` (bukan cuma enqueue `thoth run` telanjang). Mengangkat sebagian "Fase 3 config/profiles" dari status ditunda.*
  - **Worker `push_params`** ✅ — `JobSpec.params` (JSON, TANPA migration/perubahan skema `thoth-jobs`) dipetakan ke flag `thoth run` di `execute_pipeline` SEBELUM `RunArgs::try_parse_from`, jadi clap tetap satu-satunya sumber default. Tabel `SCALAR` 12 knob (provider/model/max_clips/layout/language/clip_style/style_profile/social/bgm/bgm_volume/sfx_intro/headline_dur) + `keywords: string[]`→`--keywords a,b,c` + `extra_args: string[]` verbatim (escape hatch). Key tak dikenal diabaikan (forward-compat); tipe mismatch skip (graceful). Unit test round-trip argv lewat `RunArgs::try_parse_from`. File: `crates/thoth-core/src/worker/mod.rs`.
  - **Reload config per job** ✅ — `run_worker` ganti `let config` → `let mut config`; tiap job klaim reload via `pick_config(AppConfig::load(), &config)` (generic `<T: Clone>`: Ok→config baru, Err→config lama + `warn!`). Edit config.toml dari UI berlaku di job berikutnya TANPA restart worker; edit rusak tak meng-crash worker hangat. File: `crates/thoth-core/src/worker/mod.rs`.
  - **Endpoint config server** ✅ — `GET /api/config` (teks config.toml, file absen → `{text:""}`), `PUT /api/config` (`{text}` → tulis ke `AppState.config_path`; TOML invalid → 400 `{error}`, file tak berubah), `GET /api/style-profiles` (nama profil dari config; parse gagal → `[]`). KETIGANYA di dalam `route_layer(require_api_key)` (bearer). `config_path` dari env `THOTH_CONFIG` else `config.toml`. Dep baru `toml = "0.8"`. File: `crates/thoth-server/src/{auth,main,routes,lib}.rs`, `tests/routes_http.rs`.
  - **Dashboard UI** ✅ — `api.ts` tambah `type RunParams` + konst enum `PROVIDERS/WHISPER_MODELS/LAYOUTS/CLIP_STYLES` (hand-synced ke enum clap `cli.rs`) + `getConfig/putConfig/getStyleProfiles`. `RunForm` panel "Options" collapsible → hanya field non-kosong yang masuk `params` (biar clap isi default). `ConfigEditor` (textarea config.toml + Save, status inline) + toggle view Runs/Config di `App.tsx`. File: `dashboard/src/{api.ts,components/RunForm.tsx,components/ConfigEditor.tsx,App.tsx}`.
  - **Ditunda by design**: profiles/vocab/curators editor terstruktur + utoipa/OpenAPI codegen (Fase 3); sub-proyek B (scout orchestration), C (content-set UI), D (end-to-end glue) dari operator console belum mulai.
  - *Verified: `build_cuda.bat` EXIT 0 (thoth.exe + thoth-server.exe rebuilt, mtime maju 07-13 11:17→20:52, 0 warning/0 error); `cargo test --workspace` 126 passed/0 failed; `dashboard` `bun run build` 0 error (2027 modules). Eksekusi via subagent-driven-development: Task 1 kena 1 loop review (implementer haiku menyimpang dari brief — drop keywords-array+extra_args, tambah flag `source_channel` invalid; fix restore brief-verbatim). Task 5 subagent mati di session-limit → diselesaikan controller inline (fix TS2345 shadcn Select null-narrowing). UI browser end-to-end (butuh server+worker live) = manual, belum dijalankan.*
